# MEMORY.md — Długoterminowa pamięć projektu DriftScope

> Ten plik kumuluje wiedzę o projekcie. Nigdy nie usuwaj wpisów — tylko dopisuj.
> Każdy wpis oznaczaj datą w formacie [YYYY-MM-DD].

---

## Architektura

**[2026-05-26] PIVOT STRATEGICZNY — kierunek: predykcja na bazie anomalii**
User zakwestionował założenie ~50% take rate jako potencjalnie mylące (nie zweryfikowane empirycznie — pochodzi z ogólnych szacunków dla loterii europejskich, nie z oficjalnych danych EuroJackpot). Decyzja: na początku kolejnej sesji obmyślić plan rozszerzenia architektury DriftScope od audytu stacjonarności → w kierunku predykcji liczb opartej na wykrytych anomaliach statystycznych (niejednorodność, autocorrelacja, periodyczność). Zachować obecny audit framework jako warstwę bazową. Kluczowe pytania: (1) rzeczywisty take rate z oficjalnych raportów, (2) minimalne effect size eksploatowalne predykcyjnie, (3) komponenty do dodania: posterior Dirichlet per liczba, rolling window, Kelly criterion, alert system.

**[2026-05-26] W1 DONE — core/ + h1_classical.py + testy 27/27**
Zaimplementowano: `core/{config,types,seeds,guards}.py`, `methodology/h1_classical.py`, `ingestion/lotto_scraper.load_seed_csv()`, `tests/test_h1_invariants.py` (10 testów). Wszystkie 27 testów zielone (9 skipped stubs). DoD-1a (KPSS rejects na pełnym szeregu euron) i DoD-1b (BOCPD top-5 pokrywa 2014 ±60 dni + 2022 ±30 dni) spełnione.

**[2026-05-26] pyproject.toml — hatchling jako build backend**
Wybór: hatchling (nie setuptools). Uzasadnienie: lżejszy, natywna obsługa `src/` layout bez dodatkowej konfiguracji, PEP 621 native. `driftscope = "driftscope.cli:app"` jako CLI entrypoint.

**[2026-05-26] numba==0.65.1 — PINNED (nie range)**
`numba==0.65.1` z `==` zamiast `>=0.65,<0.66`. Uzasadnienie: zweryfikowane na Win11 + numpy 2.2.5 (PoC 2026-05-17); ryzyko regresji przy patch update na Windows jest realne. Jedyna biblioteka pinned dokładnie.

**[2026-05-26] test_environment.py — zastąpiony (był PyTorch/CUDA-focused)**
Oryginalny plik sprawdzał torch, nvidia-smi, onnxruntime — nieistotne dla CPU-only pipeline. Zastąpiony testami: Python 3.10.x, numpy 2.x, numba 0.65.x, JIT basic + cache=True, frequency vector primitive, key imports, joblib loky, MSVC Win32. Zachowany plik `scripts/smoke_test.py` jako quick diagnostic (nie pytest).

**[2026-05-26] test_vram_invariants.py — RAM invariants (nie VRAM)**
Nazwa zachowana z template; "VRAM" repurposed na RAM budget niezmienniki (§6.1). Fixtures: `sample_input` (200 DrawRecord-compatible draws), `sample_frequency_vector` (Δ⁴⁹), `load_pipeline` (placeholder dict W1/W4).

**[2026-05-29] Contract revision + W0/W1 review — Ścieżka A utrwalona**
Pivot predykcyjny [2026-05-26] ANULOWANY → Ścieżka A (audyt + 3 czary-mary: recurrence.py W6, BOCPD animation W8; information_theory/MDL → stretch). `preregistration_v2.md` (ACTIVE, v1 SUPERSEDED): korekta daty 2014-10-08→**2014-10-10**, DoD-1 positive/negative control (euronumery vs główne 1-50 jako wbudowany negative control), nowy `recurrence.py` (gap **perm-calibrated** + Nelson-Aalen + EVT Gumbel; NIE lifelines — pandas zakazany, NA ~20 LOC własny), Family B BH→**Benjamini-Yekutieli** (300→450 hyp). **DoD-4 zostaje 3/3** (MDL nie jest niezależny od MMD).
**BOCPD α/hazard zsynchronizowane prereg↔kod** (§2: α=0.1, hazard=0.005): v1 błędnie nosił α=1; faktyczna decyzja [2026-05-26] to α=0.1 (osłabienie sygnału przy α=1 potwierdzone empirycznie: max 0.14 vs 0.47). Korekta transkrypcji, NIE zmiana metody.
**W0 power preview DONE** (`notebooks/00_power_preview.py`): real n=**958** (R1=133/R2=389/R3=436, NIE 1500 — brief poprawiony); positive control POTWIERDZONY (euron [1-8]/[1-10]/[1-12] = granice reżimów); **δ=0.01 per-reżim NIEwykrywalny** global GoF (R1 power=0.10); power = co-primary deliverable; czytać jako UPPER BOUND (FDR nieskorygowane, global GoF heurystyka, tylko signal #1).
**W1 review:** rdzeń BOCPD message-passing **POPRAWNY** (R9 do obniżenia); DoD-1b ±60 dla 2014 (data-CP≈2014-11-28) vs ±30 dla 2022. 27/27 zielonych. 7 commitów d31ba66→3912916.

**[2026-05-30] W2 DriftSim + preregistration v3 + W3 calibration core**
Baseline commit całego scaffoldu (`50862ad`). **W2:** `null_uniform.py` (`9aa6265`, uczciwy null 5/50 + 2/{8,10,12}, daty synthetic R3=Tue/Fri vs R1/R2=Fri) + `planted_signals.py` (`85a46e0`, 5 sygnałów × 4 effect = 63 datasety, guard #4 seasonality-tylko-R3, refactor `synthetic_dates`/`sample_euron` współdzielone).
**`preregistration_v3.md` ACTIVE** (`8f60de2`, supersedes v2): ratyfikacja siatek effect-size które v2 §6 zostawiał nie-pinowane — **trend β ∈ {0.01,0.02,0.05,0.10}** i **seasonality c ∈ {0.01,0.02,0.05,0.10}** (mechanizm: +c piątek/−c wtorek na liczbie planted). Rewizja czysta (przed kalibracją). Pointery przepięte w CLAUDE.md+PROJECT_BRIEF.md. (Numeracja: po v2 jest **v3**, nie v4 — wcześniejsza notatka o „v4" była błędna.)
**W3 `calibration.py`** (`0218822`→`a670fb4`→`2dd2379`): detector-agnostyczny harness Monte Carlo (power/FPR) + `chi2_main_uniformity` (Family B chi², §5); domyślne n = realne 133/389/436. **Finding (empiryczny, zwalidowany):** chi² wykrywa freq_shift/trend (marginal) ORAZ autocorr/seasonality (przez nadmierną dyspersję zliczeń); **ŚLEPY tylko na pair_corr** (joint→power≈FPR). Wstępna hipoteza była odwrotna — korekta po failu testu.
**Lekcja operacyjna:** zniekształcony output narzędzi (stale/fresh interleaving, fałszywe „updated successfully"/„commit OK") → 2 przejściowo-czerwone commity W3. Weryfikować stan przez `grep`/`git log`, nie sam renderer. Końcowo zielone: 83 passed / 7 skip, HEAD `2dd2379`.

**[2026-05-31] W4 K4-MMD — implementacja + kalibracja + naprawy po code-review (NIEzacommitowane)**
`methodology/k4_mmd.py` (był stub): two-sample MMD² (Gaussian RBF, median heuristic anti-leakage na X, unbiased Gretton 2012) na frequency vectors Δ⁴⁹ per okno; null permutacyjny w `@njit(cache=True)` nad pre-policzoną macierzą Grama; `mmd_uniform_detector` wpięty w harness `calibration.py`. **Framing (decyzja usera, §3 nie pinuje X/Y):** vs uniform reference — X=okna obserwacji, Y=okna świeżego uniform 5/50 (ten sam n). RBF liczony ręcznie (NIE sklearn `pairwise_kernels` z CLAUDE.md) — celowo, dla integracji z njit; udokumentowane.
**Finding W4 (empiryczny):** MMD łapie freq_shift(~1.0)/trend(R2/R3~1.0)/autocorr(R2/R3 0.68/0.82)/seasonality(R3~0.88); **ŚLEPY na pair_corr** (≈FPR) — TAK SAMO jak chi². → osobny **test współwystąpień W6 jest KONIECZNY** (rozstrzyga otwarte pytanie z W3). null FPR ≤ 7.5% wszystkie reżimy. Tabela 95 passed / 5 skip.
**Code-review (lokalny `/code-review high`) → 4 naprawy:** (1) crash guard ≥2 okna (ZeroDivisionError w njit przy 1 oknie); (2) **pure-function reseed** — rng seedowany z hash zawartości `draws` ⊕ base_seed → detektor czystą funkcją, DoD-6 niezależny od kolejności (był stanowy rng w domknięciu); (3) footgun non-overlap — usunięty `DEFAULT_STEP=1`, `step` wymagany; (4) test FPR na wdrożonej konfiguracji window=25. Matematyka MMD²/anti-leakage/p-value potwierdzone poprawne.
**Ustalenie metodologiczne (dla prereg_v4 przed W5):** `window=200` z §3 niewykonalny na realnych danych — strumień ~958 daje przy non-overlap tylko ~4 okna (statystycznie za cienko). Wdrożona konfiguracja MMD = `window=25` (15–17 okien, czysty FPR). Spec curve §4 N∈{100,200,400} też do rewizji.

**[2026-05-31] preregistration v4 — rewizja MIESZANA (zacommitowana `cdab71b`)**
v3 SUPERSEDED. (A) INFORMOWANA real-data [JAWNIE oznaczona]: §3 window **200→25**, §4 spec curve **{100,200,400}→{15,25,40}** (200 niewykonalny na n≈958/non-overlap — korekta błędu spec, NIE dobór pod wynik; FPR≠25 do walidacji w W6). (B) CZYSTA: §5c co-occurrence test pre-rejestrowany (curveball null). (C) DOPRECYZOWANIE: §3 non-overlap (step=window) + framing vs uniform reference. Pierwsza rewizja z częścią informowaną danymi — dyscyplina §0: jawne rozdzielenie clean vs data-informed.

**[2026-05-31] W6 test współwystąpień + preregistration v5 (zacommitowane `8fb9932` prereg, `9550b51` kod)**
`methodology/cooccurrence.py` (nowy): domyka pair_corr, na który chi²(W3)/MMD(W4) ślepe. Null = **curveball swap-randomization** (njit, zachowuje OBA marginesy: wiersze=5, kolumny=częstości; łamie parowanie; łańcuch burn-in+thinning). Statystyka = **max-pair** z=(O−E)/√E + lokalizacja pary; pure-function reseed (DoD-6).
**Korekta §5c PRZED implementacją:** pre-zarejestrowana w v4 suma `Σ(O−E)²/E` matematycznie ROZMYWA rzadki sygnał (1 para z 1225) → power≈FPR (ta sama ślepota co chi²/MMD) + miscalib (FPR~0.17). Przełączone na max-pair (poprawna kalibracja FPR≈α). Lekcja: statystyki walidować PRZED zamrożeniem (powtórka W3).
**Re-design pair_corr §6:** stary mechanizm (lift) był PODWÓJNIE wadliwy — (1) below detection floor (forced-frac=(lift−1)·0.00816 ∈ {0.0008..0.0082}, analog W0 δ=0.01), (2) PRZECIEKAŁ do marginesów (P(planted)=0.1+0.9p) łamiąc deklarację §6 o izolacji. Nowy: **margin-preserving** mieszanka 3-komponentowa (force-para p / force-brak-obu 9p / uniform 1−10p), parametr forced-frac {0.01,0.02,0.05,0.10}. Zachowuje WSZYSTKIE marginesy DOKŁADNIE (P(dowolnej)=0.1), podnosi P(i,j razem) ~12× przy p=0.10.
**Showcase (zwalidowany, czysta komórka Disagreement Protocol):** co-occurrence JEDYNYM detektorem pair_corr — power p≥0.05 → ≥0.80 wszystkie reżimy (R1@0.05=0.80; Decision Gate >70% spełniony); chi²=0.06/MMD=0.03 przy p=0.10 = FPR floor (dowodliwie ślepe, marginesy uniform); cooc FPR(null)=0.03. Uzasadnia DoD-4=3/3 (3 rodziny NIE-redundantne). preregistration **v5 ACTIVE** (po v4), +§6.5 tabela komplementarności. Suite 104 passed / 5 skip.

**[2026-05-31] W6 — recurrence + multiple_testing + permutation; DoD-2 i DoD-3 DOMKNIĘTE**
Dokończono rdzeń metodologiczny W6 — cztery komplementarne rodziny detektorów (wszystkie czyste funkcje DoD-6, FPR≈α, wspólny harness `calibration.Detector`):
- **`recurrence.py`** (`598a25d`, §5b) — rodzina TEMPORALNA. gap GoF: KS gapów vs Geometric(q=0.1), **draw-order shuffle** perm-calibrated (NIE analityczny KS — niewazny dyskretnie), omnibus max_k + lokalizacja liczby. + Nelson-Aalen (~20 LOC własny, bez lifelines) + EVT max-gap Gumbel. Kluczowe: shuffle zachowuje liczność → WARUNKUJE na marginesie → ślepy na freq_shift(0.03)/trend(0.10)/pair_corr(0.00), łapie autocorr (R3 ρ=.20→0.78, słabiej przy małym ρ).
- **`permutation.py`** (`36044c2`, **DoD-2**) — kanoniczny silnik: `permutation_pvalue` prymityw (1+#≥)/(B+1) + **lag-1 serial overlap** (statystyka ZALEZNA OD KOLEJNOSCI; PoC chi² na zliczeniach był niezmienniczy na permutację wierszy = degeneracja). njit hot loop (wzorzec PoC). FPR(null)=0.050 ✓ DoD-2. **Najsilniejszy autocorr detektor** (ρ=.05/.10/.20→1.00; recurrence dawał 0.05 przy ρ=.05 — komplementarne, nie redundantne, różne mechanizmy/granularność).
- **`multiple_testing.py`** (`6021c0a`, §5/**DoD-3**) — Family A (BH primary + Storey własny ~15 LOC, secondary) / Family B (Benjamini-Yekutieli primary + BH secondary; Storey odrzucony). `FDRResult` (q-values+maska); BH/BY przez statsmodels. Tu spłyną per-number p-values z chi²/gap.
**Macierz komplementarności:** freq_shift/trend→marginalna; autocorr→serial(1.0)+recurrence+chi²; seasonality→chi²/MMD+część recurrence; **pair_corr→WYŁĄCZNIE co-occurrence**. Każdy sygnał ma ≥1 championa. DoD-2/3/6 ✓.

**[2026-05-31] W6 — specification.py + block_bootstrap.py; WSZYSTKIE stuby methodology/ zaimplementowane**
- **`specification.py`** (`9c658fb`, §4) — spec curve 9 pkt (window∈{15,25,40} × bw∈{0.5,1,2}× median heuristic); kryterium stabilności znika w >2/9. Dodano `bandwidth_mult` do `mmd_uniform_detector` (backward-compat, σ=mult·median_heuristic(x), anti-leakage zachowane). **Zamyka walidację FPR N∈{15,40}** z v5 §4: NULL R3 → 0/9 significant (0/135 odrzuceń); freq_shift .10 → 9/9 stable.
- **`block_bootstrap.py`** (`c9e1a5d`) — moving block bootstrap (alternatywny null, block∈{5,10,20}), statystyka serial overlap (reuse permutation). Konserwatywny (FPR≤α=0.00); power autocorr maleje z block_size (b=20→0.85) = gradient zasięgu zależności. njit hot loop.
- **Stan:** 6 modułów metodologicznych kompletnych (cooccurrence, recurrence, permutation, multiple_testing, specification, block_bootstrap) + k4_mmd/h1_classical z W1/W4. Suite **152 passed / 4 skip**, nowe pliki ruff-czyste. Dług: repo ma 34 pre-existing ruff violations (N803/N806 notacja matematyczna w h1_classical/k4_mmd/test_mmd + I001/F401/E501 w cli/scraper/conftest) — NIE z tej sesji, osobny cleanup. Pozostało w W6: TYLKO kalibracja progu BOCPD reject_h0=0.3 (dotyka W1).

**[2026-05-31] W6 DOMKNIĘTY — kalibracja progu BOCPD + warm-up exclusion (NIEzacommitowane)**
Ostatnia pozycja W6. Magiczny `reject_h0 = max_cp_prob > 0.3` (jeden próg dla obu pól) był **fałszywie skalibrowany**: ukryty bug — pole `main` (N=50, K=5) dawało **FPR=0.77** pod nullem (0.3 ≪ p95=0.73), czyli negative control 1-50 *zawsze* zapalał. euron (N=12, K=2) FPR=0.07. Rozkład nullowy `max(cp_prob)` silnie zależy od (N, K) → próg MUSI być per-pole.
- **`_BOCPD_REJECT_THRESHOLD` (per-pole, 95. perc. nullu, FPR≈0.05):** euron **0.33** (p95=0.329), main **0.70** (p95=0.699). Reprodukcja: `scripts/calibrate_bocpd_threshold.py` (BASE_SEED=42, 200 trials, R3).
- **Warm-up exclusion (decyzja usera, zalecana — wymaga preregistration_v6):** `max(cp_prob)` liczone POMIJAJĄC pierwsze `warmup = N//K` losowań (euron=6, main=10). Powód: `max(cp_prob)` pod nullem to **transient burn-in** (argmax≈4-7, IDENTYCZNY dla n=436 i n=958 → próg length-invariant, ważny dla pełnej serii) — zanim pula symboli zostanie „zobaczona", każde losowanie wnosi nowe symbole → sztuczny spike, NIE change-point. Na realnym `main` JEDYNY ponadprogowy peak był w idx=7 (2012-05-11, burn-in); po warm-up max spada 0.770→0.208 → **clean negative control**. euron positive control nietknięty (peaki 2014-11-28/2022-03-29 są mid-series).
- **Walidacja:** euron reject=True (DoD-1b), main reject=False (nowy test `test_dod_1b_bocpd_negative_control_main`) + `test_bocpd_fpr_under_null` (euron/main FPR≈0.05, DoD-2 dla rodziny BOCPD). Suite **155 passed / 4 skip**.
- **Dług kontraktowy (NASTĘPNY KROK):** zmiana reguły reject + warm-up to zmiana w `methodology/` → wymaga `preregistration_v6.md` z revision_reason ZANIM commit. Real-data-informed (inspekcja neg-control/burn-in) — ujawnić wg dyscypliny §0. Próg czysto null-kalibrowany; warm-up strukturalnie uzasadniony. Plus I001 w test_h1_invariants.py + update CLAUDE.md (ACTIVE v5→v6).

**[2026-06-02] W6 ZACOMMITOWANY + prereg v6 ACTIVE + W7 Disagreement Protocol DONE**
Domknięty dług kontraktowy z 2026-05-31 oraz zrobiony następny milestone roadmapy.
- **`preregistration_v6.md` ACTIVE** (supersedes v5; commit `c290184`): §0 ratyfikuje regułę reject BOCPD — (A) CLEAN [null-kalibrowany na syntetyku]: prog 0.3→per-pole euron 0.33/main 0.70; (B) STRUKTURALNE [regułę potwierdziła inspekcja real neg-control — disclosed]: warm-up `N//K`. §2 dopisana, §7 log v5→v6. §3–§6.5 bez zmian, DoD-4=3/3. Pointery przepięte w CLAUDE.md + PROJECT_BRIEF.md (drzewo + linia 176). Kod kalibracji: commit `2f62c8d`. I001 był już rozwiązany (importy posortowane); pozostałe E501 = pre-existing dług spoza sesji. Usunięty cruft `PROJECT_BRIEF-3.md`.
- **W7 `reporting/disagreement.py`** (commit `f8b4a7c`, **DoD-4**): klasyfikacja sygnału wg zgodności **3 filarów** (H1 / MMD / co-occurrence) → 3/3·2/3·1/3·0/3 (§6.5). **Decyzja: 3 filary, nie 4** — recurrence(§5b)+permutation(§5) wchodzą pod filar temporalny H1, NIE jako osobny 4. (DoD-4 konsekwentnie =3/3). `classify`/`classify_from_results` (pure) + `run_pillars` (reuse `Detector`). `DisagreementVerdict` frozen. Test czystej komórki pair_corr→1/3 (tylko co-occurrence). reporting/ NIE podlega dyscyplinie prereg §0. mypy czysty, suite **165 passed / 2 skip**.
- **Dług techniczny ZAMKNIĘTY** (commity `c7bde59` refactor + `0abe92d` chore): (1) `Detector = Callable[[list[DrawRecord]], TestResult]` był re-deklarowany w **7 modułach** → skonsolidowany do `core/types.py` (jedno źródło; usuwa 6 osieroconych `from typing import Callable`; zero zmian behawioralnych). (2) **ruff 34→0**: `per-file-ignores` N803/N806 dla notacji math (k4_mmd/h1_classical/test_mmd — Gretton/Adams-MacKay), auto-fix I001×5+F401×2, zawinięte E501×4. **Pozostały dług (NIE ruszony):** pure-function reseed powielony (różne hashe per moduł — dotyka DoD-6, refaktor osobno); mypy pre-existing w k4_mmd/calibration (numba `@njit` untyped + brak scipy-stubs).
- **W7 `adaptive/honest_watchlist.py`** (commit `c12c0f0`, **DoD-5**): honest gate — `build_watchlist` zwraca watchlistę TYLKO z wzorców, które przeszły **DoD-3 (FDR q≤α)** ORAZ **DoD-4 (konwergencja ≥min_convergence)**; inaczej **None** (honest null, NIE pusta lista) + explicit message. **Primary gate = FDR; konwergencja parametrem (min_convergence=1 default)** — bo czysta komórka pair_corr to 1/3 (twardy ≥2/3 odrzuciłby realny sygnał, §6.5). Reuse `DisagreementVerdict` (DoD-4) + q_value z `multiple_testing` (DoD-3). NIE predykcja (Ścieżka A, audyt). 11 testów, mypy+ruff czyste, suite **176 passed / 2 skip**.
- **MILESTONE: wszystkie komponenty DoD-1..5 zaimplementowane.** DoD-1a/1b (H1/BOCPD) ✓ · DoD-2 (permutation) ✓ · DoD-3 (multiple_testing) ✓ · DoD-4 (disagreement) ✓ · DoD-5 (honest_watchlist) ✓.
- **W7 DoD-6 ZAMKNIĘTY** (commit `4351416`): `scripts/archive.py` (był stub) — SHA-256 manifest seed CSV + artifacts, sortowany po ścieżce → deterministyczny JSON (`write_manifest` bit-identyczny). `tests/test_reproducibility.py` (10 testów): (1) `make_worker_seeds` determinizm + niezależność strumieni; (2) **pure-function reseed** — detektory czyste funkcje draws, **call-order independence** (A nieskażone przez B między wywołaniami — seed z zawartości, nie ze stanu globalnego np.random); (3) manifest determinizm. **Pure-function reseed NIE skonsolidowany** (świadomie): schematy seedowania są RÓŻNE per rodzina — k4_mmd `SeedSequence([base,digest])` vs permutation/block/recurrence `(digest^base)&0xFFFFFFFF`; cooccurrence hashuje `_incidence_matrix`, reszta `_main_matrix`. To różne mechanizmy, nie duplikacja — konsolidacja zmieniłaby seedy. Suite **186 passed / 2 skip**.
- **MILESTONE 2: CAŁA macierz DoD-1..6 ma implementację + walidację.** Pozostała niezaimplementowana warstwa: **reporting** (plots_static/interactive, report.qmd — wciąż stuby) + **integracja end-to-end** (wpięcie 3 realnych detektorów przez `run_pillars` na euron/main → disagreement → watchlist; fabryki zwracają `Detector`, ale agregacja „H1 family"→1 werdykt to decyzja projektowa) + **W8** (BOCPD pełna macierz P(R_t) + animacja .webm).

**[2026-06-03] W7+W8 RDZEŃ — end-to-end audit + reporting + hook + CLI (MVP-complete). 6 commitów.**
Domknięta warstwa showcase. Framework jest teraz URUCHAMIALNY i daje headline na realnych danych.
- **`compute_bocpd_curve`** (`ecafee0`, refactor h1_classical): czysty accessor zwracający pełną krzywą `cp_prob` + `rl_map` + warmup + threshold (TestResult niósł tylko top-K). `run_bocpd` go wywołuje — zero zmiany zachowania. Fundament reporting + animacji W8.
- **`pipeline.run_audit`** (`364e722`, **integracja end-to-end**): orkiestrator spinający DoD-1..6. **Decyzja gating (otwarte pytanie ZAMKNIĘTE):** filar `h1` = **BOCPD per-stream** (skalibrowany per-pole, FPR≈0.05); ADF/KPSS/Welch/ACF = diagnostyka, NIE głosują (unik OR-inflacji FPR). **Decyzja granularności (otwarte pytanie ZAMKNIĘTE):** MMD/co-occurrence czytają pulę GŁÓWNĄ → pełny 3-filarowy Disagreement = **negative control** (main 1-50); euron pokryty osobno przez BOCPD = **positive control**. **Family B counter (DoD-3, częściowo domknięty):** 50 per-number exact-binomial (`count_k ~ Binomial(n, 5/50)`) + Benjamini-Yekutieli; `family_b_size` KONKRETNY zamiast referencyjnych 450 (pełne domknięcie: +gap §5b per-liczba +cooc pary, przy regime-split). **HEADLINE (realne 958 losowań):** positive control reject=True, CP 2014-11-28/2022-03-29 (ground truth); negative control **0/3** (h1/mmd/cooc wszystkie no-reject); Family B **0/50**; watchlist **None** (honest null). Framework potwierdza znany sygnał i NIE halucynuje. 8 testów (logika przez wstrzykiwane filary + money test na seed CSV).
- **`plots_static`** (`f7fddbb` + `792f01a`): `plot_bocpd_changepoints` (markery CP TYLKO ponad progiem), `plot_control_comparison` (pos euron / neg main side-by-side — wizualny dowód DoD-1; warm-up spike main w zacienionej strefie = wykluczony burn-in), `animate_bocpd_hook` (10s `.webm` VP9, sweep + zapalające się CP; fallback `.gif`). Wspólny `_render_bocpd_panel` (DRY). Agg headless.
- **`report.qmd`** (`c85b868`, draft): reproducible Quarto (jupyter python3), 5 sekcji (Finding→Method→Result→Rigor→Reproducibility), live `run_audit`, figury do `figs/`. Logika chunków zwalidowana standalone (brak quarto/jupyter lokalnie). Proza PL/nagłówki EN.
- **`cli.py` + pin click** (`271bbd2`, **W8**): `driftscope run` (był stub) wpina `run_audit` + `--figures`/`--hook`; `@app.callback()` wymusza subkomendę. Lazy importy. Suite 203 passed / 2 skip, ruff+mypy czyste.
- **Pozostałe stuby:** `plots_interactive.py` (Plotly, user wybrał static), `regime_split.py` (audyt na razie full-stream, NIE per-reżim).
- **PUSH:** repo wypchnięte na GitHub → **`https://github.com/Piotr1686/DriftScope`** (remote `origin`, branch `master`, 36 commitów). Push HTTPS bez promptu (credential manager). `MEMORY.md`/`last_session.md`/`.claude/settings.local.json` celowo NIE commitowane (stan lokalny, były `M` już na starcie sesji).

**[2026-06-04] W8 DOMKNIĘTY — report.html EN wyrenderowany + GitHub Pages live + hook embedded. 2 commity.**
Publiczny showcase działa: **https://piotr1686.github.io/DriftScope/** (Pages, source `master`/`/docs`).
- **Toolchain build (instalacja):** quarto 1.9.38 (winget `Posit.Quarto`), gh 2.93.0 (winget `GitHub.cli`), jupyter+ipykernel+nbclient+nbformat (pip trusted-host), kernel `python3` zarejestrowany. **Render:** `QUARTO_PYTHON=<miniconda python>` + `quarto render report.qmd --to html` (PDF pominięty — wymaga LaTeX). Ostrzeżenie o `fonts.googleapis.com` przy renderze = nieszkodliwy fallback SSL (AVG proxy), treść niezależna.
- **`report.qmd` (commit `b662f21`):** (1) **ścieżka seed CWD-niezależna** — `ROOT = Path(driftscope.__file__).resolve().parents[2]`, `load_seed_csv(ROOT / settings.data_seed_path)` (settings.data_seed_path jest WZGLĘDNY; quarto wykonuje chunki w katalogu .qmd → bez tego FileNotFound); figury zostają `figs/` względne (= katalog wykonania = katalog qmd → zgadza się z `![](figs/...)`; embed-resources inline'uje). (2) **pełny EN** — proza przetłumaczona; **headline (sekcja 3) i watchlist (sekcja 4) SKŁADANE w qmd z pól `AuditReport`** zamiast `report.summary()`/`watchlist_message` (te są PL i obsługują CLI — NIE tłumaczone, zero ryzyka regresji testów/CLI). `verdict.label` (`_LABELS` w disagreement.py) był już EN.
- **`plots_static.py` (commit `7ea1d66`):** renderowany tekst figur przetłumaczony PL→EN (`indeks losowania`→`draw index`, `prog reject`→`reject threshold`, `pula`→`pool`, `pole`→`field`, `BOCPD wykrywa zmiany puli`→`detects pool changes`). **Lekcja:** PNG-i miały polskie napisy ZAKODOWANE w pikselach — skan tekstu HTML (base64) ich NIE wykrywa; „raport EN" wymaga tłumaczenia też kodu figur, nie samej prozy. Komentarze/docstringi zostają PL (konwencja). Testy `test_plots_static` 6/6 (asertują tylko ścieżki/suffix, nie tekst).
- **Hook embedded (`7ea1d66`):** `animate_bocpd_hook(draws,"euron")` → `figs/hook_euron.webm` (147 KB VP9, ffmpeg z winget Links na PATH); `<video autoplay loop muted playsinline>` w sekcji 1; **embed-resources inline'uje webm jako `data:video/webm;base64,`** (pandoc osadza `<source src>` — zweryfikowane, self-contained HTML 1552 KB). ffmpeg dostępny → NIE fallback .gif.
- **Deploy:** `docs/report.html` (kanoniczne wg CLAUDE.md) + `docs/index.html` (czysty landing URL). `.gitignore` += artefakty Quarto (`figs/`, `report.html` obok qmd, `.quarto/`). Pages włączone RĘCZNIE przez web UI (user); `has_pages=True`, live HTTP 200.
- **Pozostałe stuby bez zmian:** `plots_interactive.py` (Plotly), `regime_split.py` (full-stream audit, NIE per-reżim).

**[2026-06-04] regime_split.py DONE (czysty, bez zmiany prereg) + finding licznika Family B przed v7**
Domknięty ostatni istotny stub rdzenia (warstwa danych) + zdiagnozowana rzeczywistość implementacyjna przed regime-aware run_audit.
- **`ingestion/regime_split.py`** (NIEzacommitowane): `regime_of(date)`, `split_by_regime(draws) -> dict{R1/R2/R3}`, `write_regime_parquet(...)` (Zstd). Granice = **2014-10-10** i **2022-03-25**, przedział PÓŁOTWARTY [start, koniec) — losowanie DOKŁADNIE w dacie granicznej → reżim NOWY. Granice były JUŻ pre-rejestrowane w **prereg_v6 §1** → split to czysta warstwa danych, NIE dotyka dyscypliny §0 (zero zmiany prereg). `tests/test_regime_split.py` 12/12: realne liczności **R1=133/R2=389/R3=436** potwierdzają poprawność granic; determinizm Parquet (DoD-6); ruff+mypy czyste.
- **FINDING (kształtuje prereg v7) — pre-rejestrowane "Family B = 450 = 50 × 3 testy × 3 reżimy" jest CZĘŚCIOWO ASPIRACYJNE:** tylko **exact-binomial** jest naturalnie per-liczba (50 p-values). `chi2_main_uniformity` (§5), `gap_recurrence_test` (§5b), co-occurrence (§5c) to detektory **OMNIBUS** — zwracają **1 p-value + lokalizację** na strumień/reżim, NIE 50 per-liczba. Tj. §5 zakładał chi²/gap jako per-number×50, czym implementacyjnie NIE są. Implikacja dla v7: realny per-number Family B = exact-binomial × reżimy = **150**; chi²/gap/cooc = omnibus per reżim (po 1), raportowane jako rodziny komplementarne, NIE napompowują per-number licznika. Pełne 450+pary wymagałoby refaktoru recurrence/cooc na per-number/per-pair (ryzyko miskalibracji — lekcja W3/W6: walidować statystyki PRZED zamrożeniem).
- **Decyzja Fazy 2 PENDING (user przerwał AskUserQuestion, chce doprecyzować):** (Q1) scope regime-awareness run_audit — zalecane: pos control BOCPD-euron zostaje full-stream (wykrywa zmiany MIĘDZY reżimami), neg control 3 filary + Family B per reżim (H0 §1 = stacjonarność WEWNĄTRZ reżimu). (Q2) skład Family B — zalecane: exact-binomial × reżim = 150, reszta omnibus osobno, v7 koryguje "450". **run_audit NIETKNIĘTY w tej sesji** — zmiana wymaga preregistration_v7.md (revision_reason) ZANIM commit.

**[2026-06-04] FAZA 2 DOMKNIĘTA — preregistration_v7 + regime-aware run_audit. 3 commity.**
Zamknięta decyzja Fazy 2 (user: Q1=per-reżim, Q2=exact-binomial×reżim/omnibus osobno; Family B = jedna rodzina 150 pooled BY; re-render=follow-up). Commity: `3d0c526` regime_split (warstwa danych, 12/12), `c2aabc0` prereg_v7+pointery, `765db8b` pipeline+testy+report.qmd.
- **`preregistration_v7.md` ACTIVE** (supersedes v6) — rewizja **CZYSTA** (korekta specyfikacji przy integracji, NIE informowana wynikami): (A) §5 licznik Family B **450→150** = błąd kategorii: "50 × 3 testy × 3 reżimy" zakładał chi²/gap/cooc jako per-liczba, gdy to detektory **OMNIBUS** (1 p-value/reżim); rodzina per-number = WYŁĄCZNIE exact-binomial (50×3 reżimy); chi²/gap/cooc = rodziny komplementarne raportowane osobno. (B) §1c scope **per-reżim** (neg control 3 filary + Family B per R1/R2/R3 — H0 §1 stacjonarność WEWNĄTRZ reżimu; pos control euron **full-stream** bo wykrywa przejścia MIĘDZY reżimami). + klaryfikacja §5: konwergencja DoD-4 liczy **SUROWE** per-reżim rejecty (≠ family-wise FDR; single-pillar 1/3 = NIE finding). Statystyki/nulle/progi §2–§6.5 bez zmian, DoD-4=3/3. Pointery v6→v7 w CLAUDE.md+PROJECT_BRIEF.md.
- **`pipeline.run_audit` regime-aware:** nowy `RegimeAudit` (per-reżim 3 filary+verdict); `AuditReport` zyskał `regime_audits: dict{R→RegimeAudit}`, USUNIĘTE top-level `verdict`/`negative_control` (ill-defined po per-reżim). Family B: per-number exact-binomial per reżim, **POOLED** w jedną rodzinę (labels `"Rk:number_j"`), jedna korekcja Benjamini-Yekutieli; `family_b_size` = 50×#niepustych reżimów (realnie 150). Watchlist: kandydaci z prefiksem reżimu + werdykt konwergencji SWOJEGO reżimu. `summary()`/`report.qmd` render per-reżim (literały POSITIVE/NEGATIVE CONTROL/WATCHLIST zachowane dla asercji CLI/qmd).
- **GOTCHA (utrwalony w testach):** `generate_uniform_draws` NIE respektuje granic kalendarzowych (kotwice R1=2012-03-23/R2=2014-10-10/R3=2022-03-25, ale R3 startuje wtorkami 2022-03-22 = jeszcze R2; R1/R2 z dużym n przekraczają granice) → po `split_by_regime` strumień się ROZSYPUJE. Testy orkiestracji budują strumień w obrębie JEDNEGO reżimu przez `_make_draws(start_date=...)` (R3 = `date(2022,4,1)`).
- **FINDING EMPIRYCZNY (headline na 958 realnych):** pos control reject=True, CP 2014-11-28+2022-03-29 (ground truth). Neg control per-reżim: R1=**0/3**, R2=**1/3** (co-occurrence stabilnie odrzuca, para **(10,25)**, max-pair z≈4.6, p≈0.01–0.036 we wszystkich n_perm), R3=**0/3**. Family B **0/150**, watchlist **None** (honest null). **R2 1/3 to NIE halucynacja:** P(≥1 reject z 3 reżimów cooc przy α=0.05)≈14% → spójne z trafieniem przypadkowym; Disagreement Protocol poprawnie klasyfikuje jako single-pillar "requires power context", NIE finding (watchlist wymaga FDR Family B ORAZ konwergencji — 1/3 nie przechodzi). User wybrał: surowe per-reżim rejecty + klaryfikacja v7 (NIE dodawać FDR omnibus do konwergencji). Suite **216 passed / 2 skip**, ruff+mypy czyste.
- **PUSH + RE-RENDER (follow-up domknięty):** 4 commity wypchnięte na origin/master (`7ea1d66..1ca6d47`). `report.qmd` re-renderowany przez quarto (`1ca6d47`, QUARTO_PYTHON=miniconda, n_perm=199) → `docs/report.html` + `docs/index.html` (embed-resources ~1.5 MB). Live Pages https://piotr1686.github.io/DriftScope/ pokazuje teraz per-reżim (R1 0/3 / R2 1/3 / R3 0/3, Family B /150). Render zweryfikowany grep-em PRZED kopiowaniem do docs/. `figs/` + report.html obok qmd gitignorowane (zob. .gitignore). Ostrzeżenie fonts.googleapis.com = znany nieszkodliwy fallback SSL.

**[2026-06-05] Portfolio polish — plots_interactive (ostatni stub reporting) + README + LICENSE. 4 commity (`1ca6d47..8588aca`).**
Sesja portfolio/open-source readiness po domknięciu MVP per-reżim. Zero zmian metodologii (reporting NIE podlega prereg §0).
- **`reporting/plots_interactive.py`** (`ef14153`, był stub) — interaktywne (hover/zoom/pan) odpowiedniki figur BOCPD ze `plots_static.py`: `interactive_bocpd_figure(draws, field)` + `interactive_control_comparison(draws)` (2-panelowy subplot pos/neg). Reuse `compute_bocpd_curve`+`run_bocpd`, ta sama paleta (3 hex zduplikowane — by interactive NIE ciągnął matplotlib) i semantyka markerów (CP tylko ponad progiem). Opcjonalny zapis HTML z `include_plotlyjs="cdn"`. plotly imports `# type: ignore[import-untyped]` (brak stubów, wzorzec jak scipy w pipeline.py). 5 testów smoke (struktura Figure + zapis HTML). Suite **221 passed / 2 skip**, ruff+mypy czyste.
- **`report.qmd` embed + re-render** (`d6b844a`): `interactive_bocpd_figure(euron)` wpięty w sekcję 3 (pod-nagłówek "Interactive view") jako `HTML(fig.to_html(include_plotlyjs="cdn", full_html=False))`. **KLUCZOWE (wzorzec do reużycia):** plotly.js przez CDN → `embed-resources` NIE inline'uje biblioteki, HTML rośnie tylko o **~37 KB** (1.588→1.625 MB) zamiast +3.5 MB. Chart ładuje plotly z `cdn.plot.ly` (integrity hash), `Plotly.newPlot` woła się bezpośrednio przeciw `window.Plotly` — **niezależny od requirejs** (referencje requirejs to osobny boilerplate Quarto). Ostrzeżenia CDN-fetch przy renderze = znany nieszkodliwy fallback SSL (AVG proxy, jak `fonts.googleapis.com` z W8) — quarto nie inline'uje, zostaje zdalny `<script src>`. `docs/report.html`+`docs/index.html` zweryfikowane grep-em PRZED kopiowaniem (plotly-graph-div + per-reżim R1 0/3/R2 1/3/R3 0/3 + Family B /150 + hook .webm nadal inline).
- **`README.md` przepisany** (`47b4884`): stary głosił "Work in progress — W0" przy MVP-complete/publicznym repo (front door mylący). Nowy (131 linii, wszystkie liczby zweryfikowane z kodem): headline 958 losowań, 3 filary+tabela komplementarności, dyscyplina (prereg v7 §0/kalibrowane nulle/per-reżim/DoD-6), quickstart (Win11 trusted-host note), układ, tabela DoD-1..6, stack, link live Pages, zastrzeżenie "nie predykcja".
- **`LICENSE`** (`8588aca`): plik MIT (pyproject+README deklarowały MIT, pliku brakowało). Open-source readiness W10: README ✓ + LICENSE ✓.
- **Stan stubów po sesji:** reporting RDZEŃ kompletny (plots_interactive domknięty). Pozostałe (NIE rdzeń): `demo/app.py` (Streamlit, off-stack, W9+ stretch), `db/queries.py`+`db/schema_validation.py` (warstwa SQLite nieużywana — pipeline na Parquet/CSV), `information_theory.py` (absent, stretch). W9 executive summary (roadmap, ryzyko redundancji z report.html).

**[2026-06-05] CI (GitHub Actions) + mypy --strict zielony na CALYM src. 1 commit, PR pending.**
Milestone portfolio (rygor inzynierski). Wybor usera: PR z galezi (walidacja numba-on-ubuntu PRZED master).
- **`.github/workflows/ci.yml`** (nowy): trigger push/PR na master, `ubuntu-latest` + Python 3.10, cache pip. Kroki: sanity Os 0 (`pytest tests/test_environment.py`) → `ruff check src tests` → `mypy src` → `pytest -q`. `concurrency: cancel-in-progress`. Badge w README.
- **FINDING (korekta MEMORY): `mypy src` strict NIGDY nie byl czysty na calym src** — wczesniejsze "mypy czyste" w logach sesji odnosilo sie do PLIKOW DOTYKANYCH w danej sesji (reporting/pipeline), nie do `mypy src`. Realny stan: **82 bledy** w 10 plikach (dlug numba/scipy odnotowany juz 2026-06-02). `mypy` zainstalowany = **2.1.0** (`>=1.10` ciagnie najnowszy) — 2.x agresywnie flaguje bare `np.ndarray` pod `disallow_any_generics`.
- **Hybryda doprowadzenia do zieleni (decyzja usera):** (1) **56 `type-arg`** poprawione WLASCIWIE: bare `np.ndarray` → `npt.NDArray[np.int64|float64|int8|bool_]` w 9 modulach (methodology k4_mmd/permutation/cooccurrence/block_bootstrap/multiple_testing/recurrence/h1_classical, driftsim/planted_signals, core/guards). Adnotacje typu, behawioralnie OBOJETNE (`from __future__ import annotations` → stringi; numba nie czyta adnotacji). (2) **24 strukturalnie nieuniknione** (brak stubow upstream) → config `pyproject`: `[[tool.mypy.overrides]]` `ignore_missing_imports` dla `numba`/`scipy.*`/`statsmodels.*`/`psutil`; osobny override per-modul `disallow_untyped_decorators=false`+`warn_return_any=false` dla kerneli wolajacych `@njit`/statsmodels (k4_mmd, permutation, cooccurrence, block_bootstrap, multiple_testing). Reszta strict bez zmian.
- **Cross-platform mypy (WZORZEC):** `guards.py` ma branch SIGALRM (Unix) — `# type: ignore[attr-defined]` jest POTRZEBNY na Win32 (SIGALRM nie istnieje), ale **NIEUZYWANY na Linux/CI** (istnieje) → `warn_unused_ignores` (strict) wywala na ubuntu. Fix = idiom **`# type: ignore[attr-defined, unused-ignore]`** (suppress + nie-skarz-gdy-zbedny). Zweryfikowane LOKALNIE przez **`mypy --platform linux src`** (symuluje CI na Win11). Usuniete tez 2 martwe `type:ignore` (h1_classical, pipeline — redundantne po globalnym override scipy).
- **Zero zmian metodologii** (tylko typy/dtype) → BRAK update preregistration (dyscyplina §0 dotyczy decyzji statystycznych, nie adnotacji). Walidacja: ruff + mypy (win32 ORAZ --platform linux) czyste, **pytest 221 passed / 2 skipped** (bez regresji).
- **Stan dostawy:** commit `b0b83c3` na galezi **`ci/github-actions`** (push origin OK). **PR NIEUTWORZONY** — `gh auth status` przerwany przez usera (gh auth interaktywny, jak przy Pages W8). URL do recznego PR: https://github.com/Piotr1686/DriftScope/pull/new/ci/github-actions. **OTWARTE: numba==0.65.1 na ubuntu** (pin weryfikowany tylko Win11) — rozstrzygnie pierwszy bieg CI na PR.

**[2026-06-05] CI ZIELONE + merge PR#1/#2 + PRNG benchmark (wow Opcja α) SHIPPED. master=6fd4241.**
Domkniety milestone CI ORAZ forward-strategy "wow" po MVP. Trzy commity na master: tooling `95a29b3`, CI `4d12f22` (#1), PRNG `6fd4241` (#2).
- **OTWARTE PYTANIE ZAMKNIETE: `numba==0.65.1` (numpy 2.x) DZIALA na `ubuntu-latest`** — Environment sanity + mypy --strict linux zielone w pierwszym biegu CI. Pin zostaje, ZADEN fallback (env-marker/matrix-Win) niepotrzebny.
- **CI flow (sprawdzony 2×):** gh auth login `--web` (interaktywny, user przez `! gh auth login --hostname github.com --git-protocol https --web`) → `gh pr create` → `gh run watch --exit-status` → squash+`--delete-branch`. Scope `repo` wystarcza (workflow juz na origin, scope `workflow` zbedny do PR). **UWAGA: powiadomienie tla "exit 0" NIE znaczy sukces biegu — czytac output (X/✓ per krok).**
- **Tooling sesyjny na master (`95a29b3`):** `/recover` + archive system + ignore `artifacts/*.{png,webm,gif,csv}`. Commitowany OSOBNO od feature (nie miesza concernow w PR).
- **Fix RAM test (`test_vram_invariants`):** placeholder `test_ram_baseline_below_h1_budget` mierzyl ABSOLUTNY RSS (~550 MB na Linux > 500) — przechodzil na Win11 przypadkiem. → `test_ram_h1_run_below_budget`: DELTA RSS wokol `run_all_h1` z warm-upem (izoluje JIT). Platform-odporne, realnie waliduje §6.1.
- **PRNG benchmark (`6fd4241`, #2) — GLOWNA ROBOTA, zob. pamiec agenta forward-strategy:** `ingestion/rng_streams.py` (BitStream MT19937/Xorshift64/ChaCha20 + unbiased rejection + defekt `favor=`), `reporting/prng_benchmark.py` (reuse `pipeline` battery), `scripts/prng_benchmark.py` (CLI). **WYNIK n=1500: good/crypto/real → clear, MT19937+bias(7) → FLAG (Family B 1/50 + MMD p≈0.002). co-occurrence milczy (defekt marginalny). Sensitivity ✓ Specificity ✓.** report.qmd sekcja 5 "Reusability" (live) + README + docs re-render → Pages. 18 testow, suite **239 passed/2 skip**. Zero methodology (reuse pipeline → NIE prereg §0). Inkasuje claim reusability NIST/crypto z CLAUDE.md.
- **Stub `regime_split` itd. bez zmian.** Pozostale RNG (AES-CTR) + defekt period-truncation = stretch. Node20 deprecation actions (checkout@v4/setup-python@v5) = znany follow-up do 2026-06-16.

**[2026-06-06] Live Pages zweryfikowane + Node20 deprecation domkniety (PR#3). master=fdc4138. Dwa dlugi zamkniete.**
Sesja porzadkowa (zero methodology, zero nowego kodu produkcyjnego) — domkniete dwie ostatnie pozycje dlugu po MVP/PRNG.
- **Live GitHub Pages POTWIERDZONE** (otwarte pytanie z 2026-06-05 ZAMKNIETE): deploy `pages-build-deployment` run `27039621780` → `success`, `headSha=6fd4241` (= merge PR#2). Pages serwuje bajt-w-bajt committed `docs/`, a `6fd4241:docs/{index,report}.html` zawiera sekcje „Reusability" (MT19937 ×4, PRNG ×6, Sensitivity/Specificity) → live URL pokazuje benchmark. **Sandbox blokuje HTTPS (curl exit 35 / HTTP=000); weryfikacja przez `gh run view --json headSha,conclusion` + `git show <sha>:docs/...` jest pewniejsza niz flaky WebFetch (maly model ucina dlugi raport przed sekcja 5).**
- **Node20 deprecation FIX (PR#3 `fdc4138`, squash+merge):** `ci.yml` bump `actions/checkout@v4→v5` + `actions/setup-python@v5→v6` (oba Node 24, EOL Node20 = 2026-06-16). Zero zmian funkcjonalnych (te same kroki, ten sam cache pip). **CI sam sie zwalidowal na nowych akcjach** — pelny bieg zielony 2m29s (sanity Numba+numpy2.x, ruff, mypy --strict, pytest). master fast-forward `6fd4241→fdc4138`.
- **Gotcha PowerShell:** here-string `-m @'...'@` PO `&&` w jednym wywolaniu Bash/pwsh sparsowal sie zle → literalne `@` na pierwszej/ostatniej linii commit message. Fix: `git commit --amend` z prostymi powtarzanymi `-m "..."` flagami (kazdy = osobny akapit). Dla wieloliniowych message preferowac wiele `-m` nad here-string w chainie.
- **Stan:** wszystkie „dlugi" po MVP zamkniete (live verify + CI hygiene). Pozostaly TYLKO stretche/polish: pelna piatka RNG (AES-CTR-DRBG + defekt period-truncation), W9 executive PDF, demo Streamlit, analiza pary (10,25). Framework (Sciezka A) domkniety.

**[2026-06-06] PRNG benchmark — pelna piatka: AES-CTR-DRBG + defekt period-truncation (PR#4). master=5c2237d.**
Stretch „wow" zrealizowany — showcase sensitivity/specificity rozbudowany do symetrycznej macierzy 2+2+2. Zero methodology (reuse `pipeline`, poza prereg §0).
- **`AESCtrDrbgStream`** (`ingestion/rng_streams.py`): AES-256 w trybie CTR jako DRBG (szkielet NIST SP 800-90A), keystream=enc(zer), klucz=SHA-256(seed), nonce=zera. Wzorzec jak ChaCha20Stream (`modes.CTR`). Drugi reprezentant krypto → specificity na DWOCH prymitywach (strumieniowy ChaCha vs blokowy AES).
- **Defekt period-truncation** (`draws_from_stream(period=p)`): generator o krotkim okresie — pierwsze `p` losowan = cykl powtarzany do `n_draws`. Czestosci ZAMROZONE na wartosciach cyklu → przy n≫p odchylenia narastaja ~(n/p)× ponad szum binomialny → NADMIERNA DYSPERSJA zliczen (efektywna proba=p, nie n). **Inny mechanizm niz `favor`** (cala dystrybucja vs jeden numer) → wykluczaja sie (ValueError gdy oba). `period=None` → `i % n_unique == i` → zachowanie bit-identyczne ze starym (zero regresji).
- **WYNIK n=1500 (money run):** good (MT/Xorshift) + crypto (ChaCha20/AES-CTR-DRBG) + real → clear; **bias(7) → FLAG WASKI** (Family B 1/50 + MMD p≈0.002, cooc milczy); **period(50) → FLAG SZEROKI** (Family B **27/50** + MMD + co-occurrence p≈0.002). **Kluczowy punkt: framework rozroznia TYP defektu** — bias trafia wasko (1 numer), period zamraza caly rozklad i zapala 3/3 filary. Sensitivity ✓ Specificity ✓.
- **Walidacja:** suite **246 passed / 2 skip** (+7), ruff(src/tests)+mypy --strict czyste, CI ubuntu zielone 2m53s, `docs/` re-render (Quarto), Pages deploy success. README tabela 2+2+2 + test count 239→246. CLI `--period`.
- **Gotcha Pages-deploy:** wbudowany workflow `pages-build-deployment` (tryb „Deploy from branch") uzywa wewnetrznie `checkout@v4`/`upload-artifact@v4` (Node20 annotation) — to NIE nasz `ci.yml` (juz zbumpowany). Wyciszenie = przelaczyc Pages na source „GitHub Actions" + wlasny `actions/deploy-pages`. Opcjonalne.

**[2026-06-06] FINDING: db/ (SQLite access layer) — rozbieznosc KONTRAKT↔IMPLEMENTACJA. ✓ RESOLVED.**
Diagnoza przed ew. cleanupem stubow. `src/driftscope/db/` = puste stuby (queries.py 6 LOC same docstringi, schema_validation.py 4 LOC, schema.sql 30 LOC tabel, __init__ pusty). **Zero importow w `src/` i `tests/`** — warstwa nigdy nieuzyta.
- **PROJECT_BRIEF.md wplata db/ w 8+ miejscach** — w tym NARRACJA data-flow §(linia ~219): „`db/` jest terminalnym sinkiem... Reporting czyta przez `db/queries.py`, nie bezposrednio z plikow". Implementacja W7/W8 **ominela to calkowicie** — reporting/pipeline czytaja CSV/Parquet bezposrednio. SQLite (`regime_meta.sqlite`) nigdy nie zmaterializowane.
- **To NIE trywialny cleanup** — usuniecie = kontraktowa rewizja architektury (CLAUDE.md drzewo + PROJECT_BRIEF linie 13/72/145-148/169/219/443/546). Wymaga explicit update commit z rationale (zasada §0 kontraktu).
- **Rekomendacja (do decyzji usera):** usunac db/ + zsynchronizowac kontrakt z rzeczywistoscia (rationale: „access layer SQLite nigdy nie zmaterializowal sie; persystencja przez Parquet/CSV; usunieto by kontrakt==kod") — dla portfolio uczciwosc kontrakt↔kod > martwe stuby. Alternatywa: zostawic oznaczone „deferred".
- **✓ WYKONANO [2026-06-06 sesja 2]:** user wybral „usunac + sync kontrakt". `git rm -r src/driftscope/db/` (4 pliki). Kontrakt zsynchronizowany: PROJECT_BRIEF.md (nota rewizji §0 z `revision_reason`, §3 storage→Parquet/CSV inline, §4.1 drzewo, §4.2 DAG→`artifacts/`, §4.3 konwencje data-flow, §10 synergy table, DoD-6) + CLAUDE.md (Storage, Nigdy, drzewo, lfs glob) + `scripts/archive.py` (usuniety martwy glob `*.sqlite`). Zachowane: §3-alt odrzucone alternatywy (SQLite-as-primary-cache — slusznie odrzucony, wzmacnia decyzje). Walidacja: import OK, mypy --strict src czyste (32 plikow), ruff src/tests/archive.py czyste, suite green. Linie 91-92 brief = odrzucone alternatywy, nietkniete.

**[2026-06-06] Toolchain: LaTeX/PDF niedostepny (executive summary W9).**
`pdflatex`/`tlmgr` NIE na PATH; quarto bez tinytex. Prawdziwy PDF wymaga `quarto install tinytex` (~100 MB, ryzyko bledow SSL przez AVG-proxy jak przy renderze fonts/CDN). **Rekomendacja dla W9: executive summary jako print-friendly standalone HTML one-pager** (Quarto→HTML + print-CSS, eksport Ctrl+P→PDF) — zero zaleznosci LaTeX. PDF commitowany tylko jesli user zaakceptuje ryzyko tinytex.

**[2026-06-06 sesja 2] ✓ W9 executive summary ZBUDOWANY (commit `0b32711`, pushed).**
`docs/executive_summary.html` — standalone, self-contained, recruiter-facing 1-page one-pager (problem → killer-result box → 3 bullety wiarygodnosci → mini-tabela PRNG → „what it demonstrates" → footer linki/stack/246 testow). **Odrebny artefakt od `report.html`** (nie skrot — inny kat: portfolio/rekruter). Liczby zaszyte STATYCZNIE (frozen headline, 958 draws) — strona NIE odpala pipeline.
- **Format = HTML** (decyzja usera: zero ryzyka LaTeX/tinytex). Druk: `@page A4` + `@media print` → eksport `Ctrl+P` → 1 strona.
- **Gotcha print-fit:** pierwszy render dawal 2 strony; dociśnięcie `@media print` (font 9pt, marginesy 8mm, ciasne odstepy) → 1 strona. **Weryfikacja: headless Edge** `msedge --headless --print-to-pdf` + zliczenie `/Type /Page` w PDF (=1). Screenshot przez `msedge --headless --screenshot`. Brak `pdftoppm` na Win11.
- **Linkowanie:** README (przy „Live report") + callout `::: {.callout-note}` w `report.qmd` → propaguje do `docs/index.html`+`report.html` przy renderze. **`docs/` to RECZNIE kopiowane artefakty** (CI=tylko lint/test, brak render-stepu; Pages=Deploy-from-branch). `reporting/report.html`+`figs/` sa GITIGNORED (base64-inline do docs/*.html via embed-resources). Re-render: `cd src/driftscope/reporting && quarto render report.qmd --to html` → kopia do `docs/index.html`+`report.html`.
- **Render gotcha:** AVG-proxy blokuje build-time fetch plotly.js (`HandshakeFailed`), ale chunk uzywa `include_plotlyjs="cdn"` → fallback do CDN runtime, embed kompletny, render exit 0. Toolchain render OK: python3 kernel + ipykernel 7.2.0 + ffmpeg.

**[2026-06-07] ✓ IT supplement (Lempel-Ziv 1976) + demo Streamlit ZBUDOWANE (commity `e60b888`+`99b4dea`, pushed).**
Wow opcja #3 (zob. pamiec agenta `audit-framework-wow-options`). Detektor `src/driftscope/reporting/information_theory.py`.
- **Decyzja architektoniczna: SUPLEMENT, NIE 4. filar Disagreement Protocol** (zostaje 3/3, h1/mmd/cooccurrence, DoD-4). `disagreement.py`, `preregistration_v7.md`, `PROJECT_BRIEF.md` — BEZ ZMIAN. Lokalizacja w `reporting/` (nie `methodology/`) celowo omija dyscypline prereg §0 (precedens: `prng_benchmark`, `disagreement`). Pamiec wow #3: „nie wprowadzac do preregistration — suplement do core".
- **Statystyka:** znormalizowana zlozonosc Lempel-Ziv 1976 (Kaspar-Schuster, `@njit cache=True`; `lz76_complexity`); bz2 ratio jako cross-check w metadata (NIE w reject_h0). `statistic`=c_norm, p liczone na surowym c (lewy ogon).
- **Null = order-shuffle** (permutacja kolejnosci BLOKOW losowan) — zachowuje marginal (multiset) i joint wewnatrz-losowania (blok 5 nienaruszony), lamie strukture MIEDZY-losowaniowa. → IT komplementarny: slepy na freq_shift (marginal→chi²/MMD) i pair_corr (joint→co-occurrence), czuly na autocorr/period.
- **Determinizm DoD-6:** `information_detector` = czysta funkcja `draws` (seed z blake2b digestu macierzy glownej ⊕ base_seed, jak `cooccurrence_detector`). RNG przez `np.random.default_rng` w petli Python; tylko `lz76_complexity` njit (bez RNG).
- **Walidacja:** FPR≤α na nullu, power na autocorr, slepota na freq_shift (test komplementarnosci), determinizm. Na baterii PRNG zapala sie WYLACZNIE na `+period(50)` (p≈0.01, nisza sekwencyjna), milczy na good/crypto/bias i realnym EuroJackpot (p≈0.75). mypy override (njit) w `pyproject` dla `driftscope.reporting.information_theory`.
- **Integracja:** kolumna IT w `prng_benchmark.py` (`it_reject`/`it_p`, wlaczone do `flagged`) + `scripts/prng_benchmark.py` CLI + `report.qmd`. **Demo** `demo/app.py` (Streamlit, grupa optional-dep `demo`, off-stack): 3 zakladki (detection matrix / entropy-lens LZ76 histogram / Turing test); buildery czyste, `st.*` pod `render()`/`__main__`; zweryfikowane `AppTest` headless (0 wyjatkow). Suite: 260 passed.

**[2026-06-07 sesja 2] IT per-regime w report.qmd + PRZEPISANY README (brief 5-modeli) + sprostowania metryk. 3 commity (`bfa6731`/`822cb36`/`0e04caf`), pushed.**
Sesja reporting/portfolio (zero methodology — reporting poza prereg §0).
- **`bfa6731` — kolumna „IT (LZ) p" per-regime w `report.qmd`** (po tabeli 3 filarow, przed §4): suplement IT czyta negative control jako clear rowniez SEKWENCYJNIE per rezim. Realne (n_perm=199): **R1=0.535 · R2=0.855 · R3=0.620, wszystkie reject=False**. Wizualnie ODREBNY od tabeli 3 filarow (wzmacnia „supplement, NIE 4. filar"; DoD-4=3/3). Reuse `information_detector()`+`split_by_regime()`. Re-render `docs/{report,index}.html`.
- **`822cb36` — PELNY rewrite `README.md`** wg `docs/research/readme_rewrite/README_REWRITE_BRIEF.md` (synteza 5 recenzji: DeepSeek/GPT/Gemini/Kimi/Qwen): lejek laik→statystyk (16 sekcji), **2 diagramy Mermaid** (przeplyw filarow + architektura, renderuja sie na GitHubie), sformulowania uczciwosciowe (hallucination=blad I rodzaju, None=abstynencja decyzyjna, quasi-ground-truth, „does not hallucinate"→„within test power α=0.05"), porownanie do NIST/Dieharder jako KOMPLEMENTARNOSC (nie „lepsi"), roadmap bez falszywych „done", About-the-author (pharma+AI/ML).
- **SPROSTOWANIA METRYK (zmierzone, dyscyplina briefu §0 — stary README mial bledy):** (1) **„~4 GB RAM" → pojedynczy audyt = ~210 MB / ~4.5 s** (n_perm=999, i5-12500H); 4 GB to budzet pelnego sweepu DriftSim (63 datasety), NIE headline audytu. (2) **LZ76 full-stream EuroJackpot p≈0.75 → realne 0.700** (przy n_perm=199; przy DEFAULT_N_PERM=999 minimalnie inne). (3) tabela PRNG przepisana z wartosciami ze SWIEZEGO renderu (nie z pamieci).
- **NUANS BOCPD (uczciwosc, utrwalony):** top-1 CP euron = **2015-01-23 (p=0.466)**, WYZSZY niz oba ground-truthy (2014-11-28 p=0.408 / 2022-03-29 p=0.399, oba w top-5 nad progiem). 2015-01-23 = aftershock ekspansji puli 2014 (nowe symbole wciaz „dochodza"). README formuluje to UCZCIWIE jako „detects change-points covering both known transitions" — NIE „top peaks" (nie zawyza). Family B = benjamini_yekutieli, 150 hipotez, 0/150.
- **`0e04caf` — archiwum:** `DriftScope_readme/` (root) → `docs/research/readme_rewrite/` (brief + 5 recenzji), mirror konwencji `docs/research/external/`. Root repo posprzatany.

**[2026-06-07 sesja 3] DECYZJA: druga gra = Multi Multi (20/80). Research zmian regul MM/Lotto/Keno. Plan implementacji → kolejna sesja.**
Pytanie usera: czy dodac druga gre (Multi Multi/Lotto/Keno) i czy EuroJackpot jest optymalny. Research sieciowy (WebSearch) + analiza.
- **USTALENIE (research): ZADNA z trzech gier nie ma udokumentowanej zmiany ROZKLADU losowania** (= brak nowego positive control na danych realnych; wartosc EJ = pula euron 8→10→12 dala distributional change-point). Lotto **6/49 niezmienne od 1957** (zmiany tylko czestotliwosc: sob.1984/+sr.1991/+wt.2007 + nazwy). Multi Multi **20/80 od 1996**, rebrand 2009-06-15 = zmiany po stronie ZAKLADU (wybor 1-10 liczb, Plus, stawki), NIE mechaniki losowania. Keno **20/70 od 2008**, zmiana 2024-06-30 = tylko sposob wyplaty. → wszystkie to CZYSTE negative controls.
- **REKOMENDACJA (zaakceptowana): Multi Multi** jako druga gra. Decyzja spada na kryterium drugorzedne (najwiecej NOWEJ informacji): MM ma **najwiekszy alfabet (80)** (vs 70 Keno, 49 Lotto), **najdluzszy strumien 2×/dzien** (duze n), **P(liczba)=20/80=0.25** najbardziej odmienne od EJ (0.10) → najostrzejszy stress-test Family B FDR + kalibracji okna MMD + skalowania Numba. Lotto ODRZUCONE (alfabet 49 ≈ main 50, powiela istniejacy negative control). Framing: „Second real-world case study: large-alphabet high-freq NEGATIVE control + scale stress-test" (symetrycznie do sekcji PRNG).
- **ZAKRES (uczciwy, potwierdzony grep-em): uogolnienie pool=50/k=5 to PRZEKROJOWY pass**, nie bolt-on — stale rozsiane po `methodology/{permutation,recurrence,k4_mmd,cooccurrence,multiple_testing,block_bootstrap}`, `reporting/information_theory`, `pipeline` (`_MAIN_POOL_SIZE`/`_P_NUMBER_PRESENT=5/50`), `core/types` (DrawRecord main_1..5/euron_1..2 → wymaga modelu zmiennej dlugosci), `driftsim`. Defaulty = wartosci EJ (zero regresji).
- **MM NIE pasuje do `run_audit`** (brak rezimow = brak zmiany regul; brak pola „euron") → sciezka negative-control-only jak `prng_benchmark` (3 filary + Family B na strumieniu MM, prawdopodobnie w `reporting/`, POZA prereg §0 — jak PRNG/disagreement). Do POTWIERDZENIA przy planowaniu: parametryzacja P_present = wlasny parametr uniform gry MM, NIE zmiana metodologii EJ → spodziewane BRAK rewizji prereg. Plan szczegolowy w `last_session.md` NASTEPNY KROK.

**[2026-06-07 sesja 4] IMPLEMENTACJA MM kroki 1-5 DONE (parametryzacja + dane + kalibracja); zostaje runner+raport.** EJ regression = 0 (260 passed przed i po; mypy --strict + ruff src czyste).
- **TECHNIKA przewodnia (kluczowa decyzja): detektory WYPROWADZAJA pool/k z REKORDOW**, nie z parametrow fabryk: `pool = draws[0].pool_size`, `k = len(draws[0].main_numbers)`. Stale `_MAIN_POOL_SIZE`/`_MAIN_DRAW` → lokale per-wywolanie (lub `mat.shape[1]` w njit). Defaulty EJ (50/5) → bit-identyczne. Dotkniete: `core/types`, `methodology/{cooccurrence,recurrence,k4_mmd,permutation,block_bootstrap}`, `reporting/information_theory`, `pipeline.family_b_per_number_pvalues`, `driftsim/calibration.chi2`, `h1_classical.run_bocpd`.
- **DrawRecord unified (zaakceptowane):** pola EJ (`main_1..5`/`euron_1..2`) → opcjonalne; +`numbers: list[int]|None` +`pool_size:int=50`; `model_validator` XOR (EJ-komplet ALBO numbers), zakres vs pool_size; `main_numbers` zwraca generic albo EJ; konstruktor `DrawRecord.generic(date,numbers,pool_size)`. Alias `Detector` bez zmian (zero retypowania).
- **MMD reference:** `mmd_uniform_detector` rozgalezia po pool — pool==50 → istniejacy `generate_uniform_draws(ref_regime)` (EJ bit-identyczne), inaczej → nowy `driftsim.null_uniform.generate_generic_uniform(n,pool,k,rng)` (bez euron).
- **BOCPD prog per-pool:** `_BOCPD_REJECT_THRESHOLD={'euron':0.33}` + nowy `_MAIN_REJECT_THRESHOLD_BY_POOL={50:0.70, 80:0.34}`. **Kalibracja MM (80,20): p95=0.3314 (n=2000, trials=200, warmup=N//K=4, FPR@p95=0.05) → round-up 0.34** (konwencja EJ: prog≥p95). **NIEINTUICYJNE: prog MM(80,20)=0.34 < main(50,5)=0.70** — wyzsze K/N (0.25 vs 0.10) = szybsze pokrycie puli = mniej falszywych spike'ow cp_prob w transiencie. Prog niezalezny od dlugosci serii (jak EJ) → n=5000 cross-check pominiety bez utraty jakosci.
- **DANE:** Tier-1 = `https://www.wynikilotto.net.pl/download/multi_multi.csv` (200 OK, ~1.45MB). Format zrodlowy `draw_no,DD.MM.YYYY,HH:MM,n1..n20,plus` (bez naglowka; godzina+plus IGNOROWANE). Konwerter `scripts/convert_mm_seed.py` → `data/seed/multimulti_history.csv` (naglowek `draw_date,n1..n20`, **16827 losowan 1996-03-18 .. 2026-06-07**, 2×/dzien → duplikaty dat OK, kolejnosc = porzadek pliku).
- **GOTCHA wydajnosc: BOCPD `_bocpd_dirichlet` jest O(T²)** (run-length bez pruningu). n=2000 kalibracja ~210s; pelny strumien MM 16827 byłby katastrofalny → **runner MM MUSI uzyc ograniczonego okna analizy** (jak PRNG n=1500), nie pelnych 16827. Smoke (ostatnie 800 MM): BOCPD main reject=False/0.221, Family B size=80/0 reject/min_q=0.959 = all-clear (oczekiwany negative control).

**[2026-06-08] MULTI MULTI GRA 2 DOMKNIETA — kroki 6+8+7 (runner + kalibracja + raport). 3 commity pushed (`0501b6b`/`b02709e`/`133300b`).**
Domkniecie drugiej gry liczbowej jako case study reusability. Push `0e04caf..133300b`, CI uruchomione.
- **Krok 6 — runner** (`0501b6b`): `ingestion/lotto_scraper.load_generic_seed_csv(path, pool_size)` (agnostyczny CSV: draw_date + dowolna liczba kolumn liczb → `DrawRecord.generic`). `reporting/multimulti_audit.py` (`run_multimulti_audit` + `MultiMultiAuditRow`): okno ostatnich 2000, BOCPD(main) + reuse `prng_benchmark.run_battery` (Family B + MMD + cooc + IT). CLI `scripts/multimulti_audit.py`. Zero nowej methodology (reporting, poza prereg §0).
- **Krok 8 — kalibracja + testy** (`b02709e`): **FINDING ROZSTRZYGNIETY** — runner dal **FLAG** przez graniczny MMD (p=0.03), nie oczekiwany all-clear. Hipoteza "MMD window=25 zawyzony przy pool=80" OBALONA solidnym FPR (`scripts/calibrate_mmd_pool.py`, 200 trials): window=25→**FPR=0.035**, w40→0.040, w50→0.050. MMD przy pool=80 dobrze skalibrowany; 2/12 z probnego = szum. **MM FLAG = podrecznikowy lone false-positive** (1/3 single-pillar, przy 5 rodzinach P(≥1)≈23%) — poprawnie wchloniety przez Disagreement Protocol, NIE finding. **Brak zmiany konfiguracji, prereg v7 nietkniety.** `tests/test_generic_pool_invariants.py` (loader + wyprowadzanie pool z rekordow). **GOTCHA danych:** MM seed ma 2 historyczne inwersje dat (2010); ogon (okno runnera) czysty; `run_multimulti_audit` sortuje po dacie defensywnie (BOCPD sekwencyjny).
- **Krok 7 — raport** (`133300b`): `report.qmd` nowa sekcja 6 "Second real-world case study — Multi Multi" (Reproducibility→7), narracja uczciwa (clear + lone-FP wchloniety). README sekcja "Reusability — a second real-world game", liczby testow 260→266. Re-render quarto (n_perm=199), `docs/{report,index}.html` zweryfikowane grep-em PRZED kopiowaniem (MM table BOCPD 0.304/MMD 0.030 True, EJ per-rezim, Family B /150, hook .webm inline, plotly CDN).
- **Stan:** 266 passed / 2 skipped, mypy --strict + ruff czyste. Live Pages zaktualizuje sie po zielonym CI.

**[2026-06-09] DECYZJE SCOPE — gra 3 zaparkowana + MM callout w exec summary (`7ed0ca6`, pushed).**
Rozstrzygniecie dwoch otwartych pytan z sesji [2026-06-08]:
- **Gra 3 (Keno/Lotto) = ZAPARKOWANA.** Reusability nasycona przy 2 grach (EJ+MM); generyczny loader pool/k gotowy, ale 3. gra ma sens TYLKO z known ground-truth change-pointem (Keno/Lotto PL go nie maja → bylby tylko kolejny negative-control demo). Robic wtedy ~1h.
- **`docs/executive_summary.html` = EJ-flagship 1-pager + JEDNO zdanie MM callout** (nie pelna sekcja — brevity 1-pagera). MM wpleciony w bullet "Performance & clean architecture" jako dowod game-agnostic na realnym 2. datasecie; odseparowany od figury "958 real draws" (EJ-only).
- **Fix correctness:** stale liczba testow exec summary 246→**268**, README 266→**268** (zrodlo prawdy = zywy `pytest --collect-only`). Zmiany czysto dokumentacyjne (HTML/MD), CI uruchomione.

**[2026-06-09 sesja 2] FIX: werdykt runnera MM = Disagreement >=2/3, nie naiwny OR (`3e70fef`, lokalny).**
Splacony dlug z 2 sesji. `MultiMultiAuditRow.verdict` (reporting/multimulti_audit.py) zwracal "FLAG" przy samotnym MMD (OR po WSZYSTKICH detektorach: BOCPD|FamilyB|MMD|cooc|IT), choc MM jest clear. Teraz werdykt = `reporting/disagreement.classify` po **3 filarach rdzeniowych** (BOCPD→`h1` / MMD / cooccurrence): `flagged = n_agree>=2`, samotny filar (1/3) = clear (oczekiwany FP przy alpha=0.05, NIE finding). Nowe properties `disagreement`/`core_fraction`. IT = suplement, Family B = osobna bramka FDR — oba w macierzy, POZA werdyktem. CLI: kolumna `core_fraction` + usuniete mylace "FLAG | Expected: clear". `BenchmarkRow.flagged` (OR) NIE ruszony — tam OR poprawny dla sensitivity/specificity na PRNG z ground-truth. +6 testow semantyki. Suite 272 passed, ruff/mypy czyste. Smoke: MMD p=0.03 (1/3) → CLEAR. report.qmd §6 bez zmian (proza juz opisywala 1/3→clear).

**[2026-06-11] CODE-REVIEW max `3e70fef` (lokalny: 9 finderow + weryfikacja + sweep) — 15 findingow, push NIEBLOKOWANY; fix poprawny.**
Werdykt: classify/PILLARS okablowane poprawnie, testy spojne, mypy/ruff czyste. Najwazniejsze findingi:
- **#1 (glebokosc fixu):** wiersz EJ `klass='real'` w benchmarku §5 ma NADAL semantyke OR (`prng_benchmark.py:57`; `report.qmd:299` — agregat Specificity JAWNIE wlacza 'real'; `scripts/prng_benchmark.py:85`; `demo/app.py:150`). Detektory deterministyczne (seed z digestu danych) → zwykly re-render bezpieczny, ale aktualizacja seed CSV / zmiana n_perm = ~10-14% szans na publikacje "EuroJackpot FLAG / Specificity ✗" wbrew sekcjom 3-4 raportu — dokladnie incydent MM.
- **#2 (koszt decyzji >=2/3):** werdykt MM trwale slepy na defekty strukturalnie-1/3: czysty pair_corr (widzi tylko cooc — `disagreement.py:18-19`) i constant-bias (tylko MMD + Family B, ktora jest POZA werdyktem) → naglowek CLEAR przy realnym defekcie, gdy `run_benchmark` ten sam strumien FLAGuje. Do udokumentowania/mitygacji (np. eskalacja przy q<<alpha w Family B), NIE revert decyzji.
- **Doc-sync przed pushem (4 szybkie):** liczniki testow 266/268→272/274 (README:49/223/316, `docs/executive_summary.html:213`); docstring `run_multimulti_audit` 'all-clear'→'clear' (`multimulti_audit.py:96`); kwalifikator historyczny w `scripts/calibrate_mmd_pool.py:3` ('dal FLAG' = pod starym OR); `report.qmd:366` — werdykt MM to statyczna proza, nie renderuje `mm.verdict`/`core_fraction` (sekcje 3/5 drukuja z kodu).
- Reszta (pre-existing/cleanup): `--window<=0` → cale 16827 draws (O(T^2) ~70x + prog 0.34 rozkalibrowany; brak walidacji argparse); `--alpha` nie rzadzi filarem BOCPD (alpha run_bocpd = koncentracja Dirichleta; brak help=); magic `>=2` poza disagreement.py (→ dodac `DisagreementVerdict.is_convergent`); 6 testow werdyktu permutacyjnie niewrazliwych na wiring filarow (brak asercji `agreeing`, brak lone-cooc/mmd+cooc); CSV `core_fraction` '1/3' = Excel koercja na date; duplikacje (regula >=2/3 w ~7 miejscach, alias `core_fraction`, formatowanie Family B X/Y).
Struktura repo: review + osobny task `TASK_REPO_STRUCTURE_OPUS48.md` (`f8a8d06`) dla Opus 4.8 (F1-F11; P0 = drzewo CLAUDE.md nieaktualne, martwa deklaracja git-lfs, brak py.typed).

**[2026-06-11 sesja 2] TASK_REPO_STRUCTURE_OPUS48 WYKONANY — 9 commitów strukturalnych, NIEpushowane. Walidacja zielona, push odłożony na jutro.**
Zrealizowany task refaktoryzacji struktury (Opus 4.8). Decyzje usera: F2=manifest, F8=minimal, F7=zostaw, F9=tak/F10=nie. Commity `74bd82d..7348fee` (HEAD=`7348fee`), 13 commitów przed origin (9 sesji + 4 wcześniejsze niepushowane: `6313461`/`f8a8d06`/`448fd1a`/`3e70fef`).
- **F3** `74bd82d` — `src/driftscope/py.typed` (PEP 561). **F4a** `1322e7a` — `poc_permutation_engine.py` → `notebooks/` + 2 ref w README przepięte. **F4b** `5cd866e` — `universal-session-setup-prompt.md` → `docs/templates/universal_session_setup_prompt.md` (snake_case). **F6** `80fb861` — `scripts/test_api_key.py` → `check_api_key.py` (audyt: czyta klucz z `.env`, ZERO sekretu hardcoded).
- **F5** `af5a2ed` — `R&D/` → `docs/research/rd_archive/`, **30 plików** znormalizowanych do snake_case (`B. Szablon DECISION_PROMPT.md`→`b_szablon_decision_prompt.md` itd.) + `README.md` z tabelą mapowania stara→nowa. Usuwa `&` z nazwy katalogu + kolizję przeznaczenia z `docs/research/`. Zero ref w kodzie.
- **F8-minimal** `73db1bd` — `[tool.hatch.build.targets.wheel] exclude=["**/*.md"]`: 7 plików `preregistration_v{1..7}.md` (jedyne `.md` w pakiecie) NIE pakują się do wheel; pliki zostają w `methodology/` (kolokacja kontraktu). DoD F8 dopuszcza „lub żadnego" (wyklucza też v7).
- **F2 — REWIZJA KONTRAKTOWA** `2c1b6d3` (revision_reason: kontrakt==kod): polityka data versioning `git lfs` → **manifest SHA-256**. git-lfs nigdy nie zmaterializowany (brak `.gitattributes`, żaden `*.parquet` nie commitowany, binaria w `.gitignore`, trackowany tylko `artifacts_manifest.json`). PROJECT_BRIEF: nota §0 [2026-06-11] + sync §3/§4.1/§6.1/§10/DoD-6 (6 linii). CLAUDE.md DoD-6. Wszystkie wzmianki `lfs` poza notami rewizji usunięte.
- **F1** `e7a6813` — drzewo CLAUDE.md przepisane **1:1 z `git ls-files`** (było mocno nieaktualne: brak `pipeline.py`, `reporting/{information_theory,prng_benchmark,multimulti_audit}`, `rng_streams`, 5 skryptów, `multimulti_history.csv`, 16 testów). Archiwa (`rd_archive` 30, `readme_rewrite` 6) zwinięte do licznika+pointera (mają README); reszta plik-po-pliku. 25 `test_*.py`+conftest. **F9** `7348fee` — `CITATION.cff` (CFF 1.2.0, „Cite this repository").
- **F7 = ZOSTAW** `scripts/scraper_selectors.md` (referencja po dokładnej ścieżce w PROJECT_BRIEF jako deliverable W0.1 + docstring `lotto_scraper.py`; żaden kod nie czyta — przeniesienie=dryf kontraktu dla kosmetyki). Odnotowane w drzewie.
- **WALIDACJA (DoD taska):** pytest **272 passed / 2 skipped** (zero regresji), `ruff check src tests` (CI scope) czyste, `mypy src` czyste (34 pliki). **Wheel** zbudowany (`python -m build --no-isolation` po `pip install build hatchling` trusted-host): `py.typed`=True, `*.md`=0, prereg=brak. `git --follow` zachowuje historię przenosin. **DŁUG (pre-existing, POZA scope CI):** `scripts/{check_api_key,smoke_test}.py` mają 4 ruff (I001 + unused `data`) — istniały przed taskiem, CI sprawdza tylko `src tests`, NIE ruszane (zmiana=semantyka kodu, poza zakresem taska strukturalnego).
- **NIEZROBIONE (NASTĘPNY KROK):** `git push` + check CI (`gh run watch`). Push wyśle 13 commitów; uwaga: pending doc-sync z drugiego taska (liczniki testów 266/268→272/274 w README/exec_summary) NADAL otwarte — task struktury celowo ich nie ruszał (sekcja 6 „Poza zakresem"). `docs/*.html` (Pages) niezmienione tym taskiem → render Pages bez zmian po pushu.

**[2026-06-13] PUSH zaległości + doc-sync + Finding #1/#2 (werdykt PRNG OR→Disagreement, honest-disclosure 1/3). HEAD=`79c03e9`, pushed, CI green.**
Sesja porządkowa po tasku struktury: domknięty NASTĘPNY KROK z 2026-06-11 + dwa findingi code-review wymagające decyzji. Reporting-only — **zero methodology, bez rewizji preregistration** (poza dyscypliną §0).
- **PUSH zaległości** (`7ed0ca6..38aec30`, 14 commitów): task TASK_REPO_STRUCTURE_OPUS48 wypchnięty, CI zielone (`27461790549`). Następnie **doc-sync** (`e890b08`): liczniki testów `266/268→272/274` (README:49/223/316 + `docs/executive_summary.html:213`), docstring `run_multimulti_audit` „all-clear"→„clear" (samotny 1/3=clear), kwalifikator `calibrate_mmd_pool.py:3` (FLAG MMD = pod starą polityką naive-OR). `docs/index.html`/`report.html` NIE niosą licznika testów → re-render zbędny dla doc-sync.
- **Finding #1 — `BenchmarkRow.flagged` OR → Disagreement ≥2** (`e553820`): PRNG benchmark werdykt był **naiwnym OR** (dowolny z 4 detektorów → FLAG), niespójnie z `MultiMultiAuditRow` (już ≥2/3 od `3e70fef`) ORAZ z własną prozą `report.qmd §5` (opisuje „3 core families + IT supplement"). Graniczny pojedynczy detektor fałszywie przerzucał real/crypto na FLAG → podkopywał claim **specificity**. Nowa polityka: **FLAG przy ≥2 z {Family B, MMD, co-occurrence}; IT non-voting** (nowe `core_votes`). **Strukturalna różnica vs MM:** PRNG battery NIE ma filaru BOCPD/h1 → oś marginalną niesie tam Family B (w MM Family B = osobna bramka FDR poza werdyktem, bo MM ma BOCPD). Sensitivity zachowana — empirycznie (n=600 i money n=1500): bias=Family B+MMD (≥2), period=3 → oba FLAG; good/crypto/real=0. **Przy n=1500 wartości kolumny Verdict bez zmian** (OR i ≥2 dają tu identyczny wynik — fix usuwa kruchość, nie zmienia headline). 4 testy regresyjne (`test_prng_benchmark.py`: lone=clear, IT non-voting, ≥2=FLAG). Komentarz MM `:47` zaktualizowany (oba werdykty spójne).
- **Finding #2 — honest-disclosure ślepoty ≥2/3 na 1/3** (`e553820`, `report.qmd §4`): sygnał widoczny z konstrukcji TYLKO jednej rodzinie (czysta korelacja par → wyłącznie co-occurrence, „czysta komórka" §6.5) pojawia się jako **1/3**. Twarda reguła „≥2/3=real" byłaby **strukturalnie ślepa** na tę klasę. Udokumentowano, że projekt świadomie NIE używa takiego progu: bramka watchlisty = **FDR (primary) + konwergencja ≥1**, więc prawdziwy single-family signal z FDR może się przebić; samotny flag BEZ FDR (R2 cooc) nie. Klasyfikacja 1/3 = routing („requires power context"), NIE odrzucenie. Bez zmian kodu.
- **Re-render Pages** (`79c03e9`): `report.qmd` (proza §4+§5) → quarto re-render → `docs/index.html`+`report.html`. **Zweryfikowane grep-em PRZED kopią:** obie nowe prozy obecne, per-reżim R1 0/3·R2 1/3·R3 0/3, Family B /150, PRNG period(50), hook .webm inline. CI green (`81182935177`). **GOTCHA narzędziowy:** `cd` w Bash tool UTRWALA katalog roboczy między wywołaniami → kolejne ścieżki względne się podwajały (`src/.../reporting/src/.../reporting/...`) dając fałszywe „No such file" w audycie /recover. Po `cd` używać ścieżek BEZWZGLĘDNYCH (`git -C`, pełne ścieżki) lub wrócić do roota.
- **Walidacja końcowa:** pytest **276 passed / 2 skipped** (+4), ruff src/tests + mypy --strict (34 pliki) czyste, CI 2× green. Liczniki podbite do **278** (po dodaniu 4 testów: README + exec_summary). Drzewo czyste, `origin/master` zsynchronizowany.

**[2026-06-13 sesja 2] Domknięcie Finding #1 (sub-decyzja Family B) + 3 polish + BOCPD length-invariance cross-walidacja. HEAD=`da3b50d`, pushed.**
Sesja domykająca cały backlog z 2026-06-13 sesja 1. Reporting/docs/style-only — **zero methodology, prereg v7 nietknięty**. 4 commity (`adb7e74`/`d2abd12`/`90e4e38`/`da3b50d`), wszystkie na origin/master.
- **Decyzja sub-Finding #1 — granularność Family B dla wiersza EJ-`'real'` w benchmarku §5** (`adb7e74`): wybór usera = **full-stream (50) + caveat**, NIE per-reżim (150). Uzasadnienie: 6/7 źródeł benchmarku to syntetyczne PRNG BEZ struktury reżimów → battery aplikowany **IDENTYCZNIE** do każdego źródła = najmocniejszy claim reusability. Per-reżim to wiedza EJ-specyficzna (znane CP) która nie generalizuje; special-case w `run_battery` złamałby genericzność. Werdykt ten sam w obu wariantach (**clear** — pula 1-50 niezmienna przez CP 2014/2022). Caveat (EN) w `report.qmd §5` + nota w docstringu `run_battery` (`prng_benchmark.py`) + zdanie w README intro tabeli. `family_b_per_number_pvalues(draws)` w benchmarku = full-stream; headline `run_audit` = per-reżim pooled BY /150 (bez zmian). Re-render `docs/{index,report}.html` (grep-zweryfikowany przed kopią).
- **Finding #2 do README** (`d2abd12`): akapit „Why we do not hard-gate on ≥2/3" (analog `report.qmd §4`) — README miał już komórkę 1/3 („not suppressed, not promoted"), ale brakło głębszego punktu strukturalnej ślepoty. README-only, poza docs/ Pages → bez re-renderu.
- **Polish 1 — ruff `scripts/{check_api_key,smoke_test}.py`** (`90e4e38`): 3× I001 (auto-fix) + 1× F841 (`data = check(...)` → `check(...)` spójnie z Test 3/4/5). Pre-existing dług poza scope CI ruff (`ruff check src tests`), sprzątnięty.
- **Polish 2 — exec summary 1×A4 ZWERYFIKOWANY** (verify-only, bez zmian): headless `msedge --print-to-pdf` → liczba `/Type /Page` (nie `/Pages`) = **1 strona**. Metoda potwierdzona (zob. też W9 [2026-06-06 sesja 2]).
- **Polish 3 — BOCPD cross-walidacja progu pool=80 przy n=5000** (`da3b50d`, komentarz w `h1_classical.py:_MAIN_REJECT_THRESHOLD_BY_POOL`): **WYNIK ELEGANCKI** — p95(n=5000, 200 trials, BASE_SEED=42) = **0.3314 = IDENTYCZNE z n=2000** (Δ=0.0000), FPR@0.34=**0.04**≤0.05. **Empirycznie domyka argument prereg v6 §0:** null max(cp_prob) MM jest zdominowany przez burn-in transient (warmup=4, argmax we wczesnej serii) → próg **length-invariant**, trzyma niezależnie od długości serii. Próg 0.34 dla pool=80 cross-zwalidowany. **Koszt:** BOCPD O(T²) bez prunningu run-length → 7s/trial @ n=5000, ~23 min/200 trials (uruchomione w tle).
- **Polska wersja README (prywatna, POZA repo):** `D:\Programming_Projects\zz_INNE\README_PL\README.md` (383 linie, tłum. z `da3b50d`). Nagłówek HTML oznacza jako prywatną/nieoficjalną; kanoniczny publiczny README zostaje EN. `zz_INNE` to katalog-rodzeństwo poza repo DriftScope → niewersjonowany, świadomie.

**[2026-06-14] Mini-study pary (10,25) w R2 — konkretyzacja jedynej flagi negative-controlu. HEAD=`bfa5754`, pushed.**
Domknięty ostatni merytoryczny stretch z backlogu (Opcja 2 z trzech kandydatów następnego kroku; Opcja 1 = hygiena Pages-deploy świadomie odrzucona jako złe ryzyko/zysk, Opcja 3 = „done"). Reporting/docs-only, **prereg v7 nietknięty, zero zmian logiki `src/`**. 1 commit (`bfa5754`), na origin/master.
- **Wynik analityczny (deterministyczny, DoD-6):** jedyna niezerowa flaga negative-controlu (`R1 0/3 · R2 1/3 · R3 0/3`) to test współwystąpień w **R2** (n=389), lokalizujący parę **(10, 25)**. To **„clean cell"**: O=9 vs E≈2.19 (z≈4.60 @ n_perm=999, ≈4.67 @ 199), ale **marginesy niewinne** — #10 ma 28 wystąpień (najrzadsza liczba w R2, oczek. 38.9, z_marg=−1.84), #25 ma 38 (idealnie średnia, z_marg=−0.15). Czysto **łączna** anomalia. Para stabilna względem n_perm (top_pair=argmax z, zdominowana przez O). Top-5 par izolowane (rank 2: (16,18) z=3.56) — brak klastra.
- **Dlaczego 1/3, nie finding (spójne, nie sprzeczne):** ta sama własność czyniąca współwystąpienia jedynym detektorem czystego `pair_corr` („clean cell", §6.5) sprawia, że spurious fluke łączny też jest niewidoczny dla per-number Family B (FDR 0/150, min q=1.0) → bramka FDR strukturalnie nie może go cross-walidować → routing do 1/3 „requires power context". Expected-rate: P(≥1 z 3 reżimów flaguje @α=0.05)=1−0.95³=**14.3%** → dokładnie jedna flaga = najbardziej prawdopodobny niezerowy wynik pod nullem. watchlist=None.
- **Deliverable:** (1) nota `docs/research/2026-06-14_r2_cooccurrence_pair_10_25.md` (PL, pełna anatomia). (2) `report.qmd §4` data-driven chunk `r2-cooccurrence-pair` — nazywa parę i liczy marginesy z `report.regime_audits["R2"]`+`draws` (odporne na drift, p NIE hardcoded). (3) re-render `docs/{index,report}.html` (grep-zweryfikowany przed kopią: `(10, 25)`, „28/38 times", z≈4.7, „P≈14%"). Render: `QUARTO_PYTHON=miniconda quarto render report.qmd --to html` n_perm=199; ostrzeżenia SSL avgMonFltProxy/cdn.plot.ly = znany nieszkodliwy fallback proxy AVG.

**[2026-06-26] Adwersarialny audyt README + korekty rzetelnościowe + batch portfolio-readiness. HEAD=`a345fb4`, pushed, CI green. 5 commitów.**
Sesja portfolio/uczciwościowa po domknięciu Ścieżki A. Reporting/docs/meta-only — zero methodology, prereg v7 nietknięty. Commity `a80e1ce`/`05570e9`/`df9c05d`/`c6420bf`/`a345fb4`.
- **Audyt 3 adwersarialnych agentów na README** (fact-check / sceptyk statystyczny / krytyk klarowności). Werdykt: liczby SOLIDNE (cała tabela PRNG 7×4 zgodna co do cyfry z `docs/report.html`, 958/133-389-436/16827/FDR 150/próg 0.34 zweryfikowane), ale overclaim przez przemilczenie + redundancja.
- **Korekty NAPRAWCZE N1–N7** (`a80e1ce`): **N1** ujawniony top-1 CP BOCPD **2015-01-23 (≈0.47)** — aftershock ekspansji 2014, WYŻSZY niż oba ground-truthy (zgodne z `report.html`/MEMORY [2026-06-07 s.2]); „invents none" → „invents none **on the control**". **N2** „three independent / no two overlap" → „**complementary**, different blind spots"; nieredundancja zawężona do co-occurrence (H1+MMD współdzielą blind-spot — własna tabela to potwierdza). **N3** Benjamini-Yekutieli „valid under **arbitrary** dependence" (było błędnie „negative dependence"; kod `multiple_testing.py:78` miał poprawnie). **N4** inline-caveat mocy przy R1 n=133 (powołanie na W0 δ=0.01). **N5** licznik ujednolicony do **278**; sprostowany split Ubuntu CI (test Windows-only skipuje → realnie ~275 pass/3 skip na Linux, nie 276+2). **N6** crypto-bullet → negative control benchmarku (n=1500 nie ma mocy odróżnić crypto od ideału). **N7** „bit-identical" → „**in the same pinned environment**".
- **Polish KLAROWNOŚCI K1/K3/K7/K8** (`df9c05d`): K1 skrócony akapit hard-gate (9→6 linii), K3 „Beyond the lottery" 6→3 domeny + linia zbiorcza, K7 quickstart bez redundantnego `--n-perm 999`, K8 roadmap „streaming MMD". **Świadomie pominięte:** K4 (re-ordering sekcji — ryzyko zepsucia celowego lejka z 5 recenzji), K5 (mermaid różne poziomy), K2/K6 (resztki na różnych wysokościach / mianownik już wyjaśniony).
- **Batch portfolio-readiness:** (1) **About przez `gh repo edit`** — description + homepage (Pages) + 12 topiców (było PUSTE: `description=""`, `topics=null`). (2) **`pyproject` metadane** (`05570e9`): `[project.urls]` (5), `keywords` (12), `classifiers` (8). **GOTCHA TOML:** `[project.urls]` wstawione PRZED `dependencies` → parser wciągnął całą listę zależności do tabeli `urls` (zepsułoby pakiet); tabele muszą iść PO bare-keys — przeniesione na dół, zwalidowane `tomli` przed commitem. (3) **Release `v0.1.0`** (tag + `gh release create` z notami). (4) **Social card** `docs/assets/social_preview.png` 1280×640 (`c6420bf`, matplotlib, paleta `#0EA5E9/#94A3B8/#EF4444`, hero-sparkline BOCPD). (5) **Community health** (`a345fb4`): `CONTRIBUTING.md` (gates + reguła prereg §0) + PR template + 2 issue-forms YAML + config.
- **FINDING DOSTĘP gh (utrwalone):** token zalogowany **Piotr1686, scope `repo`+`gist`+`read:org`** (keyring) — wystarcza na About/Release/push/Issues/`gh api`. **BRAK scope `workflow`** → push zmian w `.github/workflows/*.yml` byłby ODRZUCONY (istotne tylko dla opcjonalnej hygieny Pages-deploy Node20; nadanie: `gh auth refresh -s workflow`, interaktywnie). **Social preview obrazek = BRAK API GitHub** (REST i GraphQL) — funkcja wyłącznie web-UI, NIEautomatyzowalna żadnym klientem/scope → JEDYNY krok wymagający usera (Settings → Social preview → Upload). To limit GitHuba, nie uprawnień.

**[2026-06-27] Dwujęzyczny README EN/PL (układ Neural-Mosaic) + social preview wgrany. HEAD=`7a4010d`, pushed, repo synced.**
Sesja portfolio/docs-only — zero methodology, prereg v7 nietknięty. Domknięty ostatni rekomendowany krok portfolio-readiness + dwujęzyczność.
- **Social preview WGRANY** (user, web-UI) — domknięty NASTĘPNY KROK z 2026-06-26. `docs/assets/social_preview.png` 1280×640 teraz aktywny jako Open Graph card repo. Framework + portfolio w pełni domknięte.
- **`README.md` (kanoniczny EN) przepisany + `README.pl.md` (tłumaczenie 1:1) NOWY** (`7a4010d`, 631 insert / 92 del): układ wzorowany na drugim projekcie usera **Neural-Mosaic** — (1) przełącznik języka pod H1 (`**English** · [Polski](README.pl.md)` / odwrotnie), (2) jednolity zestaw **6 badge'y** (CI / MIT / py3.10 / platform Win∣Linux / last-commit / repo-size — dynamiczne shields github/*), (3) hero w `<p align="center">` = `social_preview.png` (jedyny istniejący asset graf., width=640), (4) centrowana linia linków (Live report / Executive summary / demo), (5) **ToC z kotwicami**, (6) **parytet 20 sekcji** w obu plikach (potwierdzony `grep "^## "`). **Kotwice PL zachowują polskie znaki** (`ł/ó/ą/ż/ć` zostają — konwencja GitHub: lowercase + spacje→myślniki, unicode-letters bez zmian, np. `#wydajność`/`#użycie`/`#poza-loterią`).
- **Dodane realne sekcje wyciągnięte Z KODU (nie zgadywane):** **Configuration** = 8 kluczy z `.env.example` z wartościami domyślnymi (BASE_SEED=42, DATA_SEED_PATH, ARTIFACTS_DIR, LOG_LEVEL, 3× SCRAPER_*, LOTTO_API_KEY); **Usage** = flagi CLI z `cli.py` (`--seed-csv`/`--n-perm` def 999/`--figures`/`--hook`/`--out-dir`); **Requirements** = stack z `pyproject.toml`. **Liczba testów 278 zweryfikowana `pytest --collect-only`** (235 `def test_` + parametryzacja → 278 collected).
- **Punkt 3 weryfikacji (LZ76/bz2) — opis README ZGODNY z kodem:** `reporting/information_theory.py` realnie `import bz2` (l.34) + `_bz2_ratio` w `metadata`, jawnie NIE w `reject_h0` (l.183) → README „bz2 compression-ratio **cross-check**" precyzyjne. order-shuffle null nad blokami losowań (l.163), LZ76 Kaspar-Schuster 1987 (l.75-111). Nic nie poprawiano.
- **Treść merytoryczna (958/R1-R2-R3/tabela PRNG 7×4/0-150/~4.5s-210MB/~2.7×) przeniesiona 1:1 z poprzedniego (zweryfikowanego 2026-06-26) README** — bez re-weryfikacji liczb, tylko reorganizacja w sekcje + ToC + tłumaczenie. **Push: oba commity (`5d9ccbb` session-state z poprz. sesji + `7a4010d` README) → origin/master, `## master...origin/master` (synced 0/0).** LF→CRLF warningi nieszkodliwe (Windows).

---

**[2026-06-29] Audyt README↔kod (dowodowy + 5 ról + adwersarialny) + MUST/SHOULD/NICE fixy + START migracji PL→EN całego repo. HEAD=`4ed01bb`, pushed.**
Najpierw pełny audyt z `CLAUDE_CODE_README_AUDIT_PROMPT.md`: zweryfikowano ~27 claimów ŻYWYMI uruchomieniami (pytest, run_audit, prng_benchmark). **Wszystkie liczby headline EJ broni się 1:1** (4.63s/221MB, daty CP 2015-01-23/2014-11-28/2022-03-29 @ 0.47/0.41/0.40, R1/R2/R3=133/389/436, 0/150 BY, watchlist None). **Główny finding:** `p=0.005` w tabeli PRNG to **permutation floor** (1/200, tabela gen. z n_perm=199, NIE z udokumentowanego default 499→floor 0.002), nieoznaczony. Deliverables w `docs/audit/` (`README_AUDIT.md` + `README_REVISED.md/.pl.md`, commit `c6104d5`).
- **MUST fixy (commit `5e6205b`):** tabela PRNG floor oznaczony `≤`; caveat „does not hallucinate = α=0.05, w granicach mocy" przyciągnięty do hasła §30-sec (EN+PL).
- **B — defekt kodu (commit `09b5044`):** `FAMILY_B_SIZE 450→150` w `multiple_testing.py` (stała niosła starą numerację v5; prereg v7 §0(A) skorygował — per-number = tylko exact-binomial 50×3; chi²/gap/cooc = omnibus osobno). README (150) było poprawniejsze niż kod.
- **SHOULD/NICE (commity `f17242a`+`28b50aa`):** mini-słowniczek, sekcja „What you'll see" z realnym outputem, złagodzone „must agree" (informatywna zgodność, nie twardy próg), DoD-6 „pinned env" + manifest nad input seed CSV, FPR=0.035 z notą MC/CI, „Beyond the Lottery" podniesione, linki do testów, **nowy `test_mmd_blind_to_pair_corr`** (pair=0.02 vs freq=1.00 — domyka symetrię claimu komplementarności H1+MMD ślepe). **Output CLI/raportu przełączony PL→EN** (`pipeline.summary()`, komunikaty watchlisty, echo cli) — zachowane tokeny POSITIVE/NEGATIVE CONTROL + „honest null" (asercje test_cli/test_pipeline trzymają). Testy: **279 collected (277 pass/2 skip)**.
- **DECYZJA: cały projekt → angielski** (user, 2026-06-29), za wyjątkiem `README.pl.md`. Odwraca konwencję CLAUDE.md „Język komentarzy w kodzie: polski" (do flipu przy domknięciu migracji). Zakres A „shipped+active": 70 .py (src/tests/scripts/demo) + report.qmd + prereg_v7 + re-render HTML. **Moje (asystenta) odpowiedzi do usera ZOSTAJĄ PL** („Język komunikacji" ≠ artefakty). **Postęp i18n: ZROBIONE+pushed** `core/` (4) + `ingestion/` (3) + `methodology/` (8) = 15 plików (commity `1606e64`/`c67eeb4`/`4ed01bb`). Proza+komentarze+komunikaty błędów EN, kod 1:1, ruff+mypy+testy zielone po każdym batchu. **Gotcha:** komunikaty błędów matchowane w testach (np. `non-overlap` w k4_mmd) — zachować token; line-length E501 po translacji (długie PL→EN docstringi) — skracać.

**[2026-07-19] World Lottery Audit (Ścieżka A) SHIPPED — ślepa replikacja na Powerball + Mega Millions; i18n tests/ domknięte. HEAD=`6cd9f8d`, pushed, CI zielone.**
Sesja: (1) dokończony Batch 5 i18n — `tests/` 20 plików PL→EN (`7b992a7`, 277 pass); (2) decyzja usera po audycie generalizowalności: wdrożenie „w boju" = **A (World Lottery Audit) + B (RANDAO/beacony) sekwencyjnie** — 9 linków AI/Kaggle usera odrzuconych metodologicznie (brak struktury k-z-N z uniform nullem, naciąganie łamie prereg §0); (3) cała ścieżka A w jednej sesji (`ad741e8` dane, `5dd9942` runner+progi, `6cd9f8d` raport/README/deploy).
- **Dane:** oficjalne CSV z data.ny.gov (Socrata: PB `d6yy-54nr` 1968 losowań 2010–2026, MM `5xaw-6ayf` 2520 losowań 2002–2026) → `scripts/convert_ny_lottery.py` → seedy generic (main+bonus) w `data/seed/`. Ładowane z pool=max historyczne; 1 wiersz PB (2018-01-06) miał niesortowane białe (artefakt wpisu NY, wartości poprawne — konwerter sortuje defensywnie).
- **WYNIK (4488 losowań, blind): 4/4 udokumentowanych zmian matrycy wykryte przez ≥1 filar, 0 spurious onsetów, delta matrycy odzyskana co do symbolu.** MM 56→75 (2013-10-22): BOCPD onset **day-zero** + kontrast FB {57..75}; MM 52→56 (2005): FB {53..56}; MM 75→70 shrink (2017): BOCPD MISS strukturalny, FB **vanished {71..75}**; PB 59→69 (2015): BOCPD near-miss (max 0.474, empiryczne **p=0.0647**), FB {60..69}.
- **Coverage warm-up (kluczowa poprawka reużywalności, warstwa reporting, poza prereg §0):** domyślny warm-up `N//K` dla pul k=5 rażąco nie pokrywa transientu coupon-collector (~(N/k)·ln N losowań; nullowe mean max 0.43→0.19 po poprawce). Nowy warm-up `ceil((N/k)·H_N)` (`lottery_audit.coverage_warmup`); progi kalibrowane PRZY nim: pool 69→**0.49** (p95=0.4815), 75→**0.39** (p95=0.3858), wpisane do `_MAIN_REJECT_THRESHOLD_BY_POOL` z adnotacją (caller MUSI podać zgodny warmup).
- **Onset-based localization:** sygnatura ekspansji rozmywa się miesiącami (ostatni debiut nowego symbolu: PB +81 dni, MM +189 dni; potem re-ekwilibracja posterioru) → lokalizacja = pierwsze przekroczenie progu per segment, attribution window 365 dni po zdarzeniu; argmax MM (0.721 @ 2014-07-04) to echo, NIE osobna anomalia.
- **Asymetria BOCPD (finding do raportu):** natychmiastowa reakcja na NOWY symbol (szok likelihood przy zero-count), niemal ślepy na WYCOFANIE (dowód z nieobecności wolny) — shrink niesie dwustronny exact-binomial Family B. Komplementarność Disagreement zreplikowana na realnym ground truth.
- **Deliverables:** `reporting/lottery_audit.py` (+`EventAudit`/`LotteryAuditRow`, kontrast FB pre/post odzyskujący deltę matrycy), `scripts/lottery_audit.py` (CLI), `tests/test_lottery_audit.py` (5, w tym kontrakty replikacyjne per gra), sekcja 7 `report.qmd` (live chunk), README EN+PL tabela 4/4, `docs/` re-render (webm+plotly zweryfikowane), push, CI ubuntu zielone 2m25s. Suite **284 collected (282 pass/2 skip)**.
- **Gotcha SSL (AVG MITM):** curl/httpx+certifi padają (exit 35 / CERTIFICATE_VERIFY_FAILED); działa `httpx.get(verify=ssl.create_default_context())` — kontekst z magazynu systemowego Windows (zawiera root AVG). Wzorzec do scraperów na tej maszynie.
- **Zostało w A/B:** B = RANDAO/beacony (adapter BitStream; task #9); stretch A: strumienie bonus (k=1: cooc N/A), UK Lotto (licencja mirrorów TBD), rodzynki rygoru (Bułgaria 2009, RPA 2020, moc na fraud Tiptona) — NIE weszły do raportu.

**[2026-07-20] Ścieżka B: B2 (beacony) SHIPPED, B3 (RANDAO) w toku — skan checkpointowany. HEAD=`4f368b5`, 4 commity ahead origin, NIE pushed.**
Sesja: research źródeł → B2 w całości → B3 spec+infra, skan przerwany na życzenie usera (20% zrobione, wznawialny).
- **KOREKTA PLANU (najważniejsza decyzja sesji):** zapisany plan „`BeaconStream` wg `BitStream` → `draws_from_stream` → bateria, cel = audyt manipulowalności RANDAO" jest **metodologicznie błędny i został odrzucony**. RANDAO mix to XOR hashy BLS; proposer withholding wybiera 1 z 2^k **uniformnych** kandydatów wg użyteczności zależnej od przydziału obowiązków w epoce e+2, NIE od bitów → rozkład brzegowy miksu pozostaje pod atakiem uniformny → bateria uniformności ma **moc zerową** wobec tego ataku. Ten sam rodzaj nieadekwatności, za który odrzucono 9 linków AI [2026-07-19].
- **Właściwy obserwowalny (B3):** **pozycja pominiętego slotu wewnątrz epoki** (pula 32, uniform null = kontrakt frameworka). Withholding zostawia ślad jako brak bloku; wartość manipulacyjną ma tylko ogon epoki (atakujący musi PRZEWIDZIEĆ mix → żadna nieprzewidywalna kontrybucja nie może nastąpić po nim) → sygnatura = nadmiar na pozycji 31, zanikający ku przodowi.
- **Konfundent pozycji 0 (znaleziony w probie PRZED projektem testu, disclosed):** przejście epoki (justification/finalization/shuffling) liczy się na slocie 0, wolniejsze węzły go gubią → podwyższony miss rate bez związku z RANDAO. Pozycja 0 to zarazem NAJGORSZY slot do withholdingu → wyjaśnienie łagodne i adwersarialne wskazują **przeciwne końce epoki**, co czyni test ogonowy czystym. Wykluczona z referencji nullu, raportowana osobno.
- **Prerejestracja — decyzja usera:** BEZ `preregistration_v8.md`. Prereg pozostaje kontraktem metodologii EuroJackpota; B3 to inna domena. Dowód kolejności daje **commit specyfikacji przed danymi** (`606dee3`, zacommitowany gdy skan jeszcze trwał i wyniki nie były widziane) — to jest istota prerejestracji, bez churnu kontraktu. Wzorzec jak `prng_benchmark`/`lottery_audit` (reporting/, poza §0).
- **Moc jako obowiązkowy element (nie dodatek):** wynik „clear" bez mocy jest bezwartościowy. Raportujemy wiązanie: przy 3000 epok / ~484 pominięciach wykrywamy z mocą 80% atakującego wstrzymującego **1 blok na 259 epok** (φ≈0.024 pominięć sterowanych atakiem). Skalowanie √n: 3× dane = 1.9× czułość. Zamienia „nie znaleźliśmy nic" w „ograniczyliśmy powierzchnię ataku".
- **B2 (SHIPPED, `fd79f61`):** drand (League of Entropy, progowy BLS, 30 s, 32 B/runda) + NIST Beacon 2.0 (entropia sprzętowa, 60 s, 64 B/puls) jako wiersze PRNG benchmark, klasa `beacon`. **Oba `clear` na wszystkich 4 osiach** (drand FB 0/50, MMD 0.914, cooc 0.674, IT 0.288; NIST 0/50, 0.580, 0.950, 0.478). Sens: zamyka zarzut „testowaliście generatory, które sami zbudowaliście, z ziarna, które sami wybraliście". RANDAO świadomie POZA tabelą (uzasadnienie wyżej + stan przycięty).
- **Beacon nie ma ziarna → DoD-6 inaczej:** odtwarzalność opiera się na **zacommitowanym cache'u digestów**, nie na `BASE_SEED`. Wyczerpanie strumienia rzuca `BeaconExhaustedError` zamiast zawijać — ciche zawinięcie sfałszowałoby dokładnie defekt period-truncation wstrzykiwany jako kontrola pozytywna.
- **Dostępność danych (zmierzone):** stany beacon chain **przycięte** — `/eth/v1/beacon/states/{slot}/randao` 404 już ~100 slotów wstecz (historyczny mix nieosiągalny bez archive node); **nagłówki i bloki sięgają ≥2 mln slotów** (~9 mies.) → B3 wykonalne. beaconcha.in (32 sloty/zapytanie) wymaga teraz klucza API — 401. Checkpoint-sync (beaconstate.info, sigp, ethstaker, ethpandaops) NIE serwują historycznych nagłówków. Zostaje `ethereum-beacon-api.publicnode.com`.
- **Rate limit — pułapka pomiarowa:** burst 300 zapytań daje 138–201 req/s bez błędu, ale **podtrzymany** skan dostaje 429 po ~1400 zapytaniach przy ~100 req/s; 25–44 req/s czyste. Mierzyć rate **podtrzymany**, nie burst.
- **Bug AIMD (znaleziony i naprawiony w locie, `8afe77a`):** limiter z samym halvingiem na 429 i bez odzysku **zabetonował** skan: cztery 429 zbiły 35→4 req/s na stałe (log: `1600 slots (35 req/s)` → `3200 slots (4 req/s)`), zamieniając ~45-min zadanie w wielogodzinne. Fix: AIMD (halving na 429 + addytywny przyrost po serii 150 sukcesów, cap na target) — po poprawce ~19 req/s przy celu 20. Lekcja: **czysty backoff bez odzysku jest błędem w każdym długim zadaniu sieciowym**, dlatego TCP używa AIMD.
- **Reguła poprawności ingestion (zadziałała realnie):** 404 = pominięty slot, cokolwiek innego (429, błąd transportu, 5xx) = **nie wiemy** → retry, a po wyczerpaniu prób `SlotScanError` i przerwanie skanu. Nigdy zapis jako pominięcie — to sfabrykowałoby dokładnie sygnaturę, której szukamy. Pierwszy pełny skan padł na 429 zamiast po cichu zatruć histogram. Pokryte `httpx.MockTransport` (offline).
- **Stan skanu:** `data/seed/randao_missed_slots.csv` + `randao_scan_meta.json` — **19200/96000 slotów (600/3000 epok), 71 pominięć, UNTRACKED (świadomie: dane częściowe nie należą do kanonicznego `data/seed/`)**. `ScanMeta.cursor` + `.complete` rozróżniają skan częściowy od pełnego. Wznowienie: `python scripts/fetch_beacon_chain.py --resume --rate 20 --out data/seed`.
- **Deliverables:** `ingestion/beacon_streams.py`, `ingestion/beacon_chain.py`, `reporting/randao_audit.py`, 3 CLI (`fetch_beacons`, `fetch_beacon_chain`, `randao_audit`), `tests/test_beacon_streams.py` (12) + `tests/test_randao_audit.py` (16). Suite **284→312 collected**. ruff+mypy --strict czyste w zakresie CI (`src tests`; `scripts/` poza zakresem — zastane 9 błędów w `make_readme_assets.py` NIE blokuje).

**[2026-07-21] B3 (RANDAO) SHIPPED — skan ukończony, audyt `clear`, sekcja 8 raportu. HEAD=`a2a83b0`, pushed, CI zielone. Całe wdrożenie A+B domknięte.**
Wznowienie checkpointowanego skanu → audyt → report/README/docs → push+CI. Zero zmian metodologii (reporting, poza §0). Def. testu nietknięta — spec `606dee3` przed danymi = dowód kolejności.
- **Skan ukończony (`a0a258b`):** `--resume` podjął od kursora (19200) do końca. Final: **96000 slotów [14717024, 14813024), 3000 epok, 358 pominięć (0,373%)**, `cursor==end_slot` (complete). Realny podtrzymany rate **~40 req/s** po rozpędzeniu AIMD (nie 19 zakładane) → ~30 min zamiast 65. Bug AIMD z [2026-07-20] potwierdzony naprawiony (zero zabetonowania). Skan częściowy→pełny NIE złamał testów (fixture'y syntetyczne/tmp_path, completeness testowane na obiektach ScanMeta, nie na committowanym pliku).
- **Wynik audytu → `clear`** (`scripts/randao_audit.py`): primary poz. 31 = **9 vs 8,1 oczek., p=0,426**; secondary ogon 28–31 = **34 vs 32,5, p=0,418**; omnibus Family B odrzuca **TYLKO pos_0** (106 pominięć = **13× średnia referencyjna** = konfundent przejścia epoki, przeciwny koniec niż withholding — dokładnie jak przewidziano [2026-07-20]). **Moc:** przy 80% wykryłby atakującego wstrzymującego **1 blok / 332 epoki** (9 wstrzymań, 3,6% pominięć). Realna moc bliska pre-szacunkowi (259 epok przy zakładanych ~484 pominięciach; faktycznie 358 pominięć → 332). Wynik null zaraportowany Z mocą — bez tego bezwartościowy.
- **Sekcja 8 `report.qmd` „Beacon withholding" (`a2a83b0`):** żywy chunk `load_scan → audit_randao` renderuje primary/secondary/omnibus + wiązanie mocy; Reproducibility (DoD-6) przenumerowana **§8→§9**. Kontrast dowodowy: bateria uniformności (§5) czyta drand/NIST jako clear, ale ma moc ZERO wobec withholdingu → B3 wymaga własnego obserwowalnego. Chunk zwalidowany standalone PRZED renderem (lekcja W3/W6). Re-render Quarto (QUARTO_PYTHON=miniconda) zweryfikowany grep-em PRZED kopiowaniem do `docs/{report,index}.html` (oba 1,65 MB); ostrzeżenia plotly CDN = znany nieszkodliwy fallback AVG proxy. README EN/PL: konkretny wynik + moc dopisane do akapitu „RANDAO deliberately absent from PRNG table".
- **Stan skanu (rewizja decyzji [2026-07-20]):** `data/seed/randao_missed_slots.csv`+`randao_scan_meta.json` teraz **TRACKED i complete** (poprzednio untracked-częściowy). Kompletny skan należy do kanonicznego `data/seed/`. `artifacts/randao_audit.csv` gitignorowany (odtwarzalny przez CLI).
- **Backlog:** i18n Batch 6 (`scripts/` EN, flip konwencji CLAUDE.md, prereg_v7, demo) + stretch A (bonus k=1, UK Lotto — licencja mirrorów TBD) pozostają otwarte. Zob. [[battle-deployment-a-plus-b]] (zaktualizowany: B3 SHIPPED).

---

## Rozwiązane problemy

**[2026-06-05] cryptography uzyte ale niezadeklarowane → CI mypy --strict fail na ubuntu (Win11 zielone)**
`ingestion/rng_streams.py` importuje `cryptography.hazmat...` (ChaCha20), ale dep NIE byla w `pyproject`. Lokalnie Win11 zielone (cryptography zainstalowane tranzytywnie/recznie) → CI nie instalowal → mypy „Cannot find ... cryptography" [import-not-found], tests step skipped. **Fix:** (1) deklaracja `cryptography>=41,<46` w `dependencies` (runtime import w testach), (2) `cryptography.*` w mypy `ignore_missing_imports` (wzorzec jak numba/scipy/statsmodels — decoupling od stub version drift). **Lekcja: nowy import third-party → sprawdz pyproject PRZED pushem. `mypy --platform linux` NIE lapie missing-import (pakiet i tak lokalnie zainstalowany) — to lapie dopiero czyste srodowisko CI.**

**[2026-06-04] winget `0x8a15005e` (msstore cert) + reuse tokenu GCM zablokowany**
(1) `winget install --id X -e` na tej maszynie failuje exit 94: „msstore: The server certificate did not match" (ten sam brak certów systemowych co pip SSL na Win11/Miniconda). Pakiet jest w źródle `winget` — fix: **`--source winget`** (pomija msstore). (2) winget zmienia PATH, ale świeży shell go NIE widzi; odświeżenie `[Environment]::GetEnvironmentVariable("Path","Machine")+";"+...("User")` **przełącza też `python`** na inny interpreter (AppData\Programs\Python310) bez driftscope/jupyter → wołać narzędzia PEŁNĄ ścieżką (`C:\Program Files\GitHub CLI\gh.exe`, `C:\Program Files\Quarto\bin\quarto.exe`) i `QUARTO_PYTHON=C:\Users\plazo\miniconda3\python.exe`, NIE nadpisywać całego PATH. (3) `gh auth login` jest interaktywny; próba wyciągnięcia tokenu z Git Credential Manager (`git credential fill` → `gh auth login --with-token`) **zablokowana przez auto-classifier** (credential reuse nieautoryzowany) — Pages włączono ręcznie przez web UI zamiast gh api.

**[2026-06-03] typer 0.12.5 ⊥ click ≥8.2 — wszystkie opcje CLI błędnie parsowane**
Objaw: `driftscope run --n-perm 49` → „Got unexpected extra argument (49)"; flagi bool → `is_bool_flag` nieustawiane („Secondary flag is not valid for non-boolean flag"); `--help` exit 1. Przyczyna: click 8.2+ wprowadził breaking change w parsowaniu opcji, którego typer 0.12.5 nie obsługuje (zainstalowany był click 8.4.1). NIE naprawia tego usunięcie `from __future__ import annotations` ani idiomatyczne flagi. Rozwiązanie: **pin `click>=8.1,<8.2`** (→ 8.1.8) w `pyproject.toml` + rationale w `PROJECT_BRIEF §3` (architectural contract). Alternatywa bump typer≥0.15 odrzucona (łamie pin typer<0.13). `pip install` wymaga trusted-host (Win11 Miniconda SSL).

**[2026-05-31] MMD sliding-window + permutacja etykiet → okna MUSZĄ być nienakładające się**
Objaw: detektor MMD z oknami nakładającymi się (step<window) dawał **FPR~1.0** (always-reject) na nullu. Przyczyna: permutacja etykiet połączonej puli okien zakłada WYMIENIALNOŚĆ; okna nakładające się są silnie skorelowane within-sample, a X⊥Y między strumieniami → wymienialność złamana → obserwowane MMD² systematycznie > null permutacyjny. Rozwiązanie: wymusić `step ≥ window` (twardy guard w `mmd_uniform_detector`). „sliding window" z §3 NIE pinuje kroku; FPR ≤ 7.5% z §3 wymaga non-overlap pod tym nullem. `window=25/step=25` → FPR ≤ 4–6% wszystkie reżimy.

**[2026-05-26] pip SSL cert failure na Win11 + Miniconda**
Objaw: `SSLCertVerificationError` przy `pip install`. Rozwiązanie: `pip install --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org`. Przyczyną jest brak certyfikatów systemowych w środowisku Miniconda na Win11.

**[2026-05-26] hatchling wymaga README.md**
`pip install -e ".[dev]"` z hatchling jako build backend failuje z `OSError: Readme file does not exist: README.md` jeśli `readme = "README.md"` w pyproject.toml a plik nie istnieje. Rozwiązanie: stworzyć README.md przed instalacją.

**[2026-05-26] Oś 0 — zielona (11/11 passed)**
`pytest tests/test_environment.py` przeszedł wszystkie 11 testów na docelowym środowisku: Python 3.10.13, NumPy 2.2.5, Numba 0.65.1, @njit basic + cache=True + frequency_vector primitive, wszystkie kluczowe importy, Polars 1.41.0, joblib loky 2-job parallel, MSVC vcruntime140.dll present.

**[2026-05-26] BOCPD — bug w message passing (Adams-MacKay 2007)**
Klasyczny błąd: changepoint term używał `logsumexp(log_R + log_pred)` (ważone po run-length), a powinien używać `log_pred_prior` (predictive pod PRIOR, niezależnie od rozkładu run-length). Skutek błędu: max_cp_prob = H = 0.005 zawsze. Fix: `log_q_cp = log_H + log_pred[0]` (counts[0] = zeros zawsze = prior).

**[2026-05-26] BOCPD alpha — default 0.1, nie 1.0**
alpha=1.0 (Laplace smoothing) za silny prior dla danych EuroJackpot: osłabia sygnał przy zmianie puli. alpha=0.1 (słabszy prior, szybsza adaptacja) daje cp_prob > 0.4 przy pierwszym niewidzianym symbolu. Z alpha=0.1 BOCPD wykrywa zmianę 1-8→1-10 na 2014-11-28 (cp_prob=0.408).

**[2026-05-26] Rzeczywiste daty change-pointów w danych EuroJackpot**
PROJECT_BRIEF wymienia "2014-10-08" i "2022-03-25" jako ground truth — są to daty ZMIANY ZASAD. Daty pierwszych OBSERWOWALNYCH zmian w danych (seed CSV):
- 2014 (1-8→1-10): pierwsza "9" pojawiła się 2014-11-28 (51 dni po zmianie zasad!) — bo losowania 10.10-21.11.2014 trafiały tylko {1-8} przypadkowo
- 2022 (1-10→1-12): pierwsza "11" pojawiła się 2022-03-29 (4 dni po zmianie zasad — niemal natychmiast)
Implikacja dla DoD-1b: tolerancja ±30 dni dla 2014 → nieosiągalna dla BOCPD na danych (używamy ±60 dni w teście).

**[2026-05-26] W0.1 DONE — API zidentyfikowane, seed CSV pobrany**
Zidentyfikowano i zdokumentowano: developers.lotto.pl API (oficjalne PTS), endpoint params, JSON structure, zakres danych (~2017-present). Seed CSV (wynikilotto.net.pl) pobrany i skonwertowany — 958 losowan 2012-2026 w `data/seed/eurojackpot_history.csv`. Deliverable: `scripts/scraper_selectors.md`. HTML scraping porzucony (ECONNREFUSED). Research blocker usunięty.

---

## Aktywne TODO (długoterminowe)

**[2026-05-26] W2 — DriftSim planted signals (następny milestone)**
W1 DONE. W2: `driftsim/null_uniform.py` (uczciwy null generator) + `driftsim/planted_signals.py` (5 sygnałów × 4 effect sizes = 20 scenariuszy, preregistration §6). Cel: calibracja sensitivity/specificity testów H1.

---

## Odrzucone podejścia

**[2026-05-26] HTML scraping eurojackpot.org — PORZUCONE**
`ECONNREFUSED` przy próbie połączenia z eurojackpot.org/archive — blokada IP/bot. Zastąpione przez oficjalne API developers.lotto.pl (Tier 2) + CSV z wynikilotto.net.pl (Tier 1).

**[2026-05-26] API developers.lotto.pl — błędne parametry**
`drawDateFrom`/`drawDateTo` (zakres dat) → 404. `index=0` → 422 (index musi być >0, 1-based). Bez `sort`/`order` → 422. Właściwe parametry: `drawDate=YYYY-MM-DD` (pojedyncza data), `index=1`, `size=1`, `sort=drawDate`, `order=asc`. gameType: musi być `EuroJackpot` (capital E i J — case-sensitive).

---

## Słownik projektu

**DrawRecord** — jedna rekord losowania: `draw_date` (ISO 8601), `main_1..main_5` (liczby 1-50), `euron_1..euron_2` (liczby 1-12 od 2022-03-25, wcześniej 1-10).

**isNewEuroJackpotDraw** — flaga w API developers.lotto.pl; `true` od 2022-03-25 (change-point: pula euroNumbers rozszerzona z 1-10 na 1-12, dwa rysowania tygodniowo zamiast jednego).

**Tier 1/2/3 ingestion** — trójpoziomowa strategia pozyskiwania danych: (1) seed CSV `data/seed/eurojackpot_history.csv`, (2) API live `developers.lotto.pl`, (3) `scripts/manual_import.py` fallback.

---

## Zewnętrzne zależności i integracje

**[2026-05-26] developers.lotto.pl — oficjalne API Polskiego Totalizatora Sportowego**
Auth: nagłówek `"secret": <LOTTO_API_KEY>` (klucz w `.env`, nigdy nie commitować).
Endpoint główny: `GET /api/open/v1/lotteries/draw-results/by-date-per-game?gameType=EuroJackpot&drawDate=YYYY-MM-DD&index=1&size=1&sort=drawDate&order=asc`
Zakres danych: ~2017-09-15 do dziś (2012-2017 niedostępne — 404).
Swagger spec: `https://developers.lotto.pl/swagger/open-api-v1/swagger.json`
SSL: `verify=False` w httpx (Miniconda Win11 brak certyfikatów systemowych).

**[2026-05-26] wynikilotto.net.pl — historyczny CSV EuroJackpot**
URL: `https://www.wynikilotto.net.pl/download/eurojackpot.csv`
Format: `draw_no,DD.MM.YYYY,m1,m2,m3,m4,m5,e1,e2` (brak nagłówka).
Zakres: 2012-03-23 .. dziś (958 losowan stan 2026-05-26). Plik committed jako `data/seed/eurojackpot_history.csv`. Aktualizacja manualna (cotygodniowa).
