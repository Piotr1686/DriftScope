```
Krytyczny audyt PROJECT_BRIEF. Solo developer, RTX 3050 (4GB VRAM),
32GB RAM, Windows 11. Cel: portfolio + narzędzie.

Załączam też HARDWARE_PUSH_CATALOG.md jako referencję.

Stwórz raport:

1. BŁĘDY FAKTYCZNE (każdy zarzut z cytatem fragmentu briefu)
2. SŁABE PUNKTY ARCHITEKTURY (nieskalowalne, over/under-engineered)
3. LEPSZE ALTERNATYWY (dla każdego zarzutu konkretne rozwiązanie)
4. BRAKUJĄCE ELEMENTY
5. UX / 10-Second Hook (czy realistyczny, czy zrobi wrażenie)
6. RYZYKA (czy szacunki czasowe realistyczne)
7. AUDYT KATALOGU
   a) Czy zastosowano kompozycję spójną z katalogiem?
   b) Czy są techniki HEAVILY relevantne, ale niezastosowane?
   c) Czy są techniki zastosowane, ale niepasujące?
   d) Czy PoC Results uzasadniają dodaną złożoność?
   e) Failure modes z katalogu — które realnie zagrażają?
8. SOURCE VERIFICATION
   Dla twierdzeń o kompatybilności: link do docs/issue
   lub oznacz jako "⚠️ UNVERIFIED — needs PoC test"

Zasady:
- Nie chwal — szukaj problemów
- Każdy zarzut z konkretną alternatywą
- Każdy zarzut musi cytować fragment briefu (eliminuje halucynacje)
- Nie proponuj rozwiązań chmurowych dla RUNTIME (build-time OK)
- Nie proponuj rozwiązań wymagających zespołu
```

ODPOWIEDŹ:
1. BŁĘDY FAKTYCZNEZarzut 1: Niewłaściwe użycie paczki mapie w kontekście szeregów czasowych/detekcji.Cytat: "Conformal inference mapie 0.8.x Pure-Python, sklearn-compatible, Vovk-style conformal p-values".Problem: mapie jest biblioteką do conformal prediction intervals (przewidywania z przedziałami ufności dla regresji/klasyfikacji), a nie narzędziem do testowania martyngałów konformalnych Vovka (conformal martingales for exchangeability/drift detection). mapie zakłada iid lub proste time-series prediction, nie liczy p-wartości w czasie do odrzucania stacjonarności procesu dyskretnego.Zarzut 2: Zła ocena własnej implementacji Bayesian Online Changepoint (BOCPD).Cytat: "Bayesian online CP (Adams-MacKay 2007, own impl) ... Własna ~150 LOC, peer-readable, testable.".Problem: Klasyczny BOCPD wymaga numerycznie stabilnego message-passingu i stosowania trików typu log-sum-exp dla funkcji hazardowych, aby zapobiec underflow/overflow prawdopodobieństw przy $N=1500$. Spakowanie tego w "150 LOC" bez gotowej wiedzy z C++ (często używanej w backandach) to prosta droga do NaN-ów, które wyjdą dopiero na Krokach kalibracyjnych.Zarzut 3: Animacja w README.md wygenerowana przez Plotly.Cytat: "Biblioteka: Plotly (go.Scatter + frames dla animacji) → eksport do .html embedowany w README przez  lub bezpośrednio jako GIF z kaleido.".Problem: GitHub Markdown odcina i blokuje tagi <iframe> oraz załączniki <script> z powodów bezpieczeństwa, więc interaktywny HTML z Plotly w README.md po prostu się nie wyrenderuje. Z kolei 15-sekundowy GIF w 60/30 fps wygenerowany przez kaleido będzie ważył dziesiątki megabajtów (brak efektywnej kompresji delta dla GIF w tym pakiecie).2. SŁABE PUNKTY ARCHITEKTURYLocking bazy SQLite przy multiprocessingu.Cytat: "SQLite jako artifact... WAL mode mandatory; jeden writer pool, multiple readers. Schemat zaprojektowany pod append-only." w połączeniu z Krok 6: "joblib.Parallel na CPU cores; SQLite cache".Zarzut: joblib z backendem loky spawnuje oddzielne procesy, które nie dzielą obiektów w pamięci w ten sam sposób co wątki. Równoległe procesy dokonujące tysięcy uderzeń (inserts/commits) per sekundę do jednego pliku SQLite wywołają błąd database is locked, nawet przy trybie WAL (który jest optymalny dla układu 1 writer / N readers, a nie N writers).DVC i bazy danych to antywzorzec.Cytat: "DVC traktuje SQLite jako binary blob — nie diff-friendly. Mitygacja: SQLite jako artifact (DVC track), nie source-of-truth".Zarzut: Aktualizacja cache’u SQLite w każdej mikrosesji testowej spowoduje, że DVC będzie przechowywać w remote'cie i w .dvc/cache całe nowe binarne kopie pliku o wadze 20 MB, co szybko spuchnie do niepotrzebnych gigabajtów i zablokuje płynność pracy.Over-engineering modułu Adaptive Watchlist.Cytat: "Krok 10 — Adaptive watchlist... opcjonalny, self-value <20% scope".Zarzut: Skomplikowanie logiki o próg FDR<0.05 tylko po to, by zwrócić None, jest sztucznym "pushem" zakresu MVP. Łamie to zasadę KISS w narzędziu, które do Krok 9 robi już pełny raport audytowy.3. LEPSZE ALTERNATYWYZamiast mapie dla konformalnych p-wartości: Zastosuj bibliotekę crepes (która obsługuje non-conformity scores) lub napisz dedykowany konformalny martyngał (Exchangeability Martingales), bazując na pakiecie awesomesauce (jeśli w Py) lub referencyjnych implementacjach testów wymiany Vovka z GitHub.Zamiast SQLite w pętli joblib: Użyj wzorca Map-Reduce. Niech poszczególne procesy zrzucają swoje wyniki (seeds, p-values) do własnych binarnych bloków, np. partycjonowanych plików .parquet (artifacts/cache/worker_id_...parquet). W fazie "Reduce" główny proces używa Polars LazyFrame do załadowania całej ścieżki i skonsolidowania wyników w locie.Zamiast własnego BOCPD: Wykorzystaj moduł ruptures (skoro i tak jest w stacku) w połączeniu z probabilistycznymi modelami cost function, lub sklonuj sprawdzoną, testowaną od lat implementację BOCPD jako git submodule, odizolowując się od ryzyka błędów numerycznych.Zamiast GIFa Plotly w README: Wygeneruj surową animację przez matplotlib.animation i wyeksportuj do pliku mp4 za pomocą ffmpeg. Przekonwertuj MP4 na lekki format .webm, który GitHub bez problemu natywnie odtworzy w tagach <video> w markdownie.4. BRAKUJĄCE ELEMENTYGenerowanie bezpiecznych PRNG w Multiprocessingu: Brakuje explicit definicji, jak izolowane są generatory liczb losowych. Jeśli joblib po prostu zforkuje/zastartuje workery bez niezależnych np.random.SeedSequence, workery Numbowej pętli będą współdzielić entropy state i wygenerują korelowane/identyczne permutacje, co fałszuje całe wyniki null distribution.Obsługa wycieku w czasie (Data Leakage) dla MMD: Brief ignoruje to, jak kernel bandwidth (mediana heurystyczna) będzie kalibrowana dla p-values predykcyjnych w streamingu na osi czasu. Jeśli zrobisz medianę z całego okresu 2014-2022 do testu zmian punktowych, to informacje z przyszłości przeciekają do parametryzacji w przeszłości.5. UX / 10-Second HookSam zarys UX w teorii ("Lotto-scam disarmer") brzmi bardzo recruitersko i profesjonalnie.Niestety jest nierealistyczny technologicznie. 15-sekundowe odpalenie Plotly GIF-a w README.md tak, by automatycznie loopowało z bezbłędnymi framami dla tysięcy punktów, spowoduje ogromne problemy wydajnościowe na przeglądarce oglądającego. Alternatywa w postaci tagu <video> (.mp4/.webm) całkowicie rozwiąże sprawę – GitHub włącza autoplay loop muted od razu, jeśli dostarczysz odpowiednio mały plik surowego wideo, robiąc dokładnie zakładane "wow".6. RYZYKAR6 (DriftSim w 20-40h) to masywne niedoszacowanie: Parametryzacja modelu, gdzie dodajesz sygnał rzędu δ=0.01 zjawiskom dyskretnym i jednorodnym (gdzie korelacja krótkozasięgowa zachowuje się w sposób mocno nieliniowy), pochłonie co najmniej dwa tygodnie pracy nad poprawnym "effect size alignment", zanim algorytmy uznają to za kalibrowalny standard.Ryzyko kompozycyjności Numba + Windows: Numba do niedawna miała wielkie problemy kompilacyjne z biblioteką Numpy w wersjach 2.x, szczególnie na Windowsie z toolchainem MSVC. Pomimo testu PoC, realna implementacja złożonej funkcji w @njit ze skomplikowanymi typami Numpy może rzucać uciążliwe błędy prekompilacji.7. AUDYT KATALOGURozumiem i szanuję decyzję architektoniczną o wyrzuceniu analizy sprzętowej: "HARDWARE_PUSH_CATALOG.md formalnie nie aplikuje (CPU-only, modele <100 MB RAM, brak ML neural).".Ale ignorując ML, brief potraktował całkowicie Oś 3 i 7 jako zarezerwowaną wyłącznie dla Deep Learningu, tracąc z oczu ogromną przewagę karty GPU.a) Kompozycja: Zastosowano brak kompozycji, co jest zgodne z założeniem wykluczenia (Oś 0 do Oś 7 wyłączone).b) Brakujące, krytyczne użycie technik z katalogu: Operacja K4-MMD (Kernel Two-Sample Test) bazuje na macierzach dystansu (Kernel Matrix). Obliczanie macierzy $N \times N$ dla przesuwnego okna to klasyczna matematyka wektorowa (BLAS). Na i5-12500H zajmie to sekundy per uderzenie, ale na RTX 3050 przez czysty torch wektorowo wykonasz uderzenie dziesiątki razy szybciej. Wyrzucenie PyTorcha "bo N<500" było fatalnym posunięciem architektonicznym przy jednoczesnym nakazie uruchamiania $10^4$ permutacji per konfig. PyTorch + Oś 3 (Compilation z trybem trace/reduce-overhead) zabiłoby ten bottleneck, zdejmując load z CPU.c/d) Wyniki PoC a katalog: PoC pokazało, że zrównoleglanie małych, szybkich jobów niszczy wydajność. Jest to zbieżne z filozofią katalogu: F6 - "każdy element wymaga przeładowania". Decyzja o przerzuceniu zrównoleglania na "poziom konfiguracji, a nie per permutacja" zamyka problem, aczkolwiek nadal GPU (z tensorami zbatchowanymi permutacji po 10k sztuk) zdruzgotałby ten wynik.e) Aktywne failure modes: F12 – Driver/DLL Conflict. Użycie Numba jako natywnego JITa na środowisku Windows 11 zawsze naraża programistę na walkę ze ścieżkami PATH do kompilatorów C/MSVC, podobnie jak ma to miejsce dla CUDNN przy instalacji bibliotek akceleracji z katalogu.8. SOURCE VERIFICATIONjoblib + sqlite lock: Znane w dokumentacji Sqlite3 ("sqlite3.OperationalError: database is locked") podczas concurrent writes bez solidnej warstwy ORM serwującej connection pool per worker, np. wbudowane funkcje SQLAlchemy.DVC + wal sqlite: ⚠️ UNVERIFIED – oficjalnie DVC traktuje wszystko co dodasz do dvc add jako md5 i deduplikuje binarnie. Problem będzie czysto przestrzenny, co zjedzie dysk 500 MB limit bardzo sprawnie w 30 iteracjach kodu.Numpy 2.x + Numba 0.65.1: ⚠️ UNVERIFIED. Informacja dodana w PoC, jednakże Numba w pełni wciąż miewa bugi kompatybilności przy wektoryzacji nowszych ABI od Numpy 2.0. Trzeba to ostrożnie otoczyć testami.Czy chcesz skupić się teraz na skorygowaniu estymacji czasowych dla modułu DriftSim, czy wolisz przearanżować architekturę zapisu, aby obejść błędy współbieżności i ograniczenia DVC?